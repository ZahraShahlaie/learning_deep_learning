بسیار عالی، در اینجا ۲۰ سؤال و جواب مصاحبه‌ای با تمرکز بر مفاهیم **محاسبه پارامترها در شبکه‌های عصبی** (FC و Conv) بر اساس محتوای ویدیوی ارائه شده، آورده شده است.

---

## 🧠 سوال و جواب‌های مصاحبه: محاسبه پارامترهای شبکه عصبی

### بخش ۱: مفاهیم پایه و اهمیت پارامترها

**۱. سوال:** منظور از **"پارامترهای یادگیری"** در یک شبکه عصبی چیست و چرا محاسبه آن‌ها اهمیت دارد؟
**پاسخ:** پارامترها همان **وزن‌ها ($W$) و بایاس‌ها ($b$)** هستند. این مقادیر تنها عناصری هستند که در طول آموزش **آپدیت** می‌شوند. اهمیت آن‌ها به دلیل تعیین **بار پردازشی**، **پیچیدگی شبکه** و **هزینه یادگیری** است.

**۲. سوال:** بار پردازشی سنگین در شبکه‌های عمیق (Deep Learning) ناشی از چیست؟
**پاسخ:** ناشی از **تعداد بالای پارامترهای یادگیری** است. هرچه شبکه عمیق‌تر شود، تعداد پارامترها افزایش یافته و به سخت‌افزار قوی‌تری برای آموزش نیاز خواهیم داشت.

**۳. سوال:** نقش یک نورون در پردازش ورودی چیست؟ دقیقاً چه پارامترهایی در این فرایند دخیل هستند؟
**پاسخ:** یک نورون یک **جمع وزنی** انجام می‌دهد: ورودی‌ها ($X$) را در **وزن‌های** متناظر ضرب کرده، با **بایاس** جمع می‌کند و از تابع فعال‌سازی عبور می‌دهد. پارامترهای دخیل، **وزن‌ها ($W$) و بایاس ($b$)** هستند.

**۴. سوال:** در یک نورون با ۵۰ ویژگی ورودی، چند پارامتر یادگیری وجود دارد؟
**پاسخ:** به ازای هر ویژگی یک **وزن** (۵۰ وزن) و یک **بایاس** داریم. در مجموع $50 + 1 = \mathbf{51}$ پارامتر یادگیری.

---

### بخش ۲: محاسبه پارامترها در لایه‌های Fully Connected (FC)

**۵. سوال:** فلسفه اتصال در یک لایه **Fully Connected** چیست؟
**پاسخ:** خروجی **هر نورون** از لایه قبلی، به **ورودی تمام نورون‌های** لایه فعلی متصل می‌شود.

**۶. سوال:** فرمول کلی محاسبه تعداد پارامترها برای یک لایه FC چیست؟
**پاسخ:** $\text{تعداد پارامترها} = (\text{تعداد یونیت‌های لایه قبلی} + 1) \times \text{تعداد یونیت‌های لایه فعلی}$

**۷. سوال:** اگر یک لایه FC شامل ۲۰۰ یونیت باشد و از لایه قبلی ۱۰۰ خروجی (ورودی) دریافت کند، تعداد پارامترهای این لایه چقدر است؟
**پاسخ:** هر نورون $100$ وزن و $1$ بایاس دارد (۱۰۱ پارامتر).
کل پارامترها: $101 \times 200 = \mathbf{20,200}$ پارامتر.

**۸. سوال:** آیا **Input Shape** (تعداد ویژگی‌های ورودی اولیه) بر تعداد پارامترهای لایه اول FC تأثیر می‌گذارد؟ چرا؟
**پاسخ:** بله، تأثیر مستقیم دارد. تعداد ورودی‌های لایه اول مستقیماً برابر با Input Shape است و هر ورودی یک وزن جداگانه نیاز دارد. 

---

### بخش ۳: محاسبه پارامترها در لایه‌های Convolutional (Conv)

**۹. سوال:** نقش **فیلتر (کرنل)** در لایه کانولوشن چیست؟
**پاسخ:** فیلتر مجموعه‌ای از وزن‌ها است که روی بخش‌های کوچک تصویر حرکت کرده و الگوهای محلی (مانند لبه‌ها، بافت‌ها) را استخراج می‌کند.

**۱۰. سوال:** در یک لایه کانولوشن، ابعاد $N \times N$ تصویر ورودی (اندازه پیکسل‌ها) چه تأثیری بر تعداد پارامترهای یادگیری دارد؟
**پاسخ:** **هیچ تأثیری ندارد**. پارامترهای یادگیری فقط به **اندازه فیلتر** و **عمق (کانال)** ورودی و **تعداد فیلترها** بستگی دارند.

**۱۱. سوال:** یک تصویر ورودی با عمق ۳ (RGB) و یک فیلتر با ابعاد $5 \times 5$ در نظر بگیرید. پارامترهای یک فیلتر چقدر است؟
**پاسخ:**
* وزن‌ها: $5 \times 5 \times \mathbf{3} = 75$ وزن (چون فیلتر باید عمق ورودی را پوشش دهد)
* بایاس: $1$ بایاس
* کل پارامتر: $75 + 1 = \mathbf{76}$ پارامتر.

**۱۲. سوال:** اگر در مثال قبل، ۱۰ فیلتر استفاده کنیم، تعداد کل پارامترهای لایه چقدر خواهد بود؟
**پاسخ:** هر فیلتر ۷۶ پارامتر دارد.
کل پارامترها: $76 \times 10 = \mathbf{760}$ پارامتر.

**۱۳. سوال:** **Stride** (گام حرکت فیلتر) و **Padding** در لایه‌های کانولوشن چه تأثیری بر تعداد پارامترها دارند؟
**پاسخ:** هیچ تأثیری بر تعداد پارامترها **ندارند**. آن‌ها فقط بر **ابعاد خروجی** (Feature Map) تأثیر می‌گذارند.

**۱۴. سوال:** دلیل عدم تأثیر ابعاد مکانی ورودی ($N \times N$) بر تعداد پارامترهای Conv چیست؟
**پاسخ:** به دلیل استفاده از تکنیک **اشتراک‌گذاری وزن (Weight Sharing)**؛ یک فیلتر واحد در تمام نقاط تصویر تکرار شده و استفاده می‌شود. 

**۱۵. سوال:** اگر خروجی یک لایه کانولوشن (Feature Map) با عمق ۶۴، ورودی لایه کانولوشن بعدی باشد، عمق فیلترهای لایه دوم چقدر باید باشد؟
**پاسخ:** عمق فیلترهای لایه دوم باید دقیقاً برابر با عمق ورودی (خروجی لایه قبلی) باشد، یعنی **۶۴**.

---

### بخش ۴: مقایسه و نکات تکمیلی

**۱۶. سوال:** تفاوت اساسی در وابستگی پارامترها بین لایه‌های FC و Conv چیست؟
**پاسخ:**
* **FC:** تعداد پارامترها **به شدت** به ابعاد ورودی (Input Shape) و ابعاد لایه قبلی وابسته است.
* **Conv:** تعداد پارامترها **فقط** به ابعاد فیلتر و عمق ورودی وابسته است و از ابعاد مکانی ورودی مستقل است.

**۱۷. سوال:** چرا لایه‌های کانولوشن نسبت به لایه‌های FC پارامترهای کمتری دارند (برای تصاویر بزرگ)؟
**پاسخ:** به دلیل **اشتراک‌گذاری وزن (Weight Sharing)**. یک فیلتر کوچک (مانند $3 \times 3$) برای کل تصویر کافی است، در حالی که در FC هر پیکسل ورودی به وزن‌های جداگانه نیاز دارد.

**۱۸. سوال:** آیا پارامترهای یک **تابع فعال‌سازی (Activation Function)** نیز آپدیت می‌شوند؟
**پاسخ:** خیر، توابع فعال‌سازی (مانند ReLU، Sigmoid و...) معمولاً **فاقد پارامترهای قابل آموزش** هستند. آن‌ها صرفاً یک عملیات غیرخطی روی خروجی جمع وزنی انجام می‌دهند.

**۱۹. سوال:** **Complexibility** یا پیچیدگی شبکه چگونه تعریف می‌شود؟
**پاسخ:** پیچیدگی شبکه با در نظر گرفتن دو عامل اصلی سنجیده می‌شود: **تعداد پارامترهای یادگیری** (وزن و بایاس) و **تعداد عملیات محاسباتی** (مانند عملیات ضرب و جمع یا FLOPS).

**۲۰. سوال:** در کجای شبکه عصبی، استفاده از لایه FC اجتناب‌ناپذیر است؟
**پاسخ:** لایه‌های FC معمولاً در **انتهای شبکه**، پس از لایه‌های کانولوشن و Pooling، برای انجام عمل **طبقه‌بندی (Classification)** نهایی استفاده می‌شوند.






بسیار عالی، در ادامه ۲۰ سؤال و جواب تخصصی دیگر در مورد مبحث محاسبه پارامترهای شبکه‌های عصبی (FC و Conv) و موضوعات مرتبط برای آمادگی کامل در مصاحبه آورده شده است.

---

## 🧠 ۲۰ سوال تخصصی دیگر: پارامترها و پیچیدگی شبکه

### بخش ۱: لایه‌های FC و انتقال پارامتر

**۱. سوال:** اگر بخواهیم یک لایه FC با ۵۰ واحد را به یک لایه FC با ۲۰۰ واحد متصل کنیم، تعداد پارامترهای وزن و بایاس بین این دو لایه چقدر است؟
**پاسخ:**
* تعداد وزن‌ها: $\text{ورودی} \times \text{خروجی} = 50 \times 200 = \mathbf{10,000}$
* تعداد بایاس‌ها: $\text{تعداد واحدهای خروجی} = 200$
* کل پارامترها: $10,000 + 200 = \mathbf{10,200}$

**۲. سوال:** فرض کنید یک شبکه عصبی کوچک دارید. اگر Batch Size را دو برابر کنید، آیا تعداد پارامترهای یادگیری شبکه تغییر می‌کند؟
**پاسخ:** خیر، **تعداد پارامترها ثابت می‌ماند.** Batch Size تنها بر **نحوه محاسبه گرادیان** و **سرعت به‌روزرسانی** پارامترها تأثیر می‌گذارد، نه بر ساختار فیزیکی (تعداد وزن‌ها و بایاس‌ها).

**۳. سوال:** اگر یک لایه FC ورودی‌ای به شکل تِنسور سه‌بعدی ($10 \times 10 \times 3$) دریافت کند، چه اتفاقی برای ورودی می‌افتد و تعداد ورودی‌های آن لایه چقدر خواهد بود؟
**پاسخ:** لایه‌های FC ورودی‌های سه‌بعدی را نمی‌پذیرند. تِنسور ورودی باید قبل از ورود به لایه FC با استفاده از عملیات **Flatten** به یک بردار یک‌بعدی تبدیل شود. بنابراین، تعداد ورودی‌ها $10 \times 10 \times 3 = \mathbf{300}$ خواهد بود.

**۴. سوال:** آیا پارامترهای یک لایه **Dropout** نیز در محاسبه تعداد کل پارامترهای شبکه لحاظ می‌شوند؟
**پاسخ:** خیر، لایه‌های **Dropout** و **Pooling** (میانگین یا ماکسیمم) **فاقد هرگونه پارامتر یادگیری** (وزن یا بایاس) هستند و صرفاً عملیات نمونه‌برداری یا تصادفی‌سازی را انجام می‌دهند.

---

### بخش ۲: لایه‌های Convolutional (Conv) و کارایی

**۵. سوال:** یک فیلتر کانولوشن $7 \times 7$ با عمق ورودی ۱۲۸ و عمق خروجی (تعداد فیلترها) ۶۴ در نظر بگیرید. کل پارامترهای این لایه چقدر است؟
**پاسخ:**
* پارامترهای یک فیلتر: $(7 \times 7 \times \mathbf{128}) + 1 = 49 \times 128 + 1 = 6272 + 1 = 6273$
* کل پارامترها: $6273 \times \mathbf{64} = \mathbf{401,472}$

**۶. سوال:** چرا لایه‌های کانولوشن از نظر پارامترها نسبت به FC در مدل‌های بینایی کارآمدتر هستند؟ (دو دلیل اصلی)
**پاسخ:**
1.  **اشتراک‌گذاری وزن (Weight Sharing):** یک مجموعه کوچک از وزن‌ها (فیلتر) در تمام نقاط تصویر استفاده می‌شود.
2.  **اتصالات محلی (Local Connectivity):** هر نورون خروجی فقط به یک ناحیه کوچک از ورودی متصل است، نه به تمام پیکسل‌ها.

**۷. سوال:** چگونه می‌توان در یک لایه Conv، تعداد پارامترهای یادگیری را بدون کاهش عمق خروجی (Feature Map) به شدت کاهش داد؟
**پاسخ:** با استفاده از فیلترهای کوچک‌تر، مانند تغییر از $5 \times 5$ به $3 \times 3$ یا استفاده از معماری‌های پیشرفته مانند **Separable Convolutions** که عملیات فضایی و عمق را جدا می‌کنند.

**۸. سوال:** آیا **Stride** بر پیچیدگی محاسباتی (زمان اجرای مدل) تأثیر دارد؟ اگر بله، چگونه؟
**پاسخ:** بله، تأثیر دارد. افزایش Stride باعث می‌شود فیلتر با گام‌های بزرگ‌تری حرکت کند، در نتیجه **ابعاد Feature Map خروجی کاهش می‌یابد** و **تعداد عملیات کانولوشن** و زمان پردازش بعدی کمتر می‌شود. 

---

### بخش ۳: پارامترها و پیچیدگی‌های عمومی

**۹. سوال:** مفهوم **Sparsity (تنک بودن)** در پارامترهای شبکه چیست و چگونه به کارایی مدل کمک می‌کند؟
**پاسخ:** Sparsity به معنی صفر بودن درصد بالایی از وزن‌های شبکه است. این امر به کاهش **حافظه مورد نیاز** برای ذخیره مدل و **کاهش بار محاسباتی** (چون ضرب در صفر حذف می‌شود) کمک می‌کند، در حالی که عملکرد مدل را حفظ می‌کند (بهینه‌سازی حافظه و سرعت).

**۱۰. سوال:** اگر در یک مدل بزرگ از **Regularization L2 (Weight Decay)** استفاده کنیم، این کار چه تأثیری بر تعداد پارامترهای مدل دارد؟
**پاسخ:** **هیچ تأثیری** بر تعداد پارامترها (وزن‌ها و بایاس‌ها) ندارد. L2 Regularization صرفاً یک **جریمه (Penalty)** به تابع زیان اضافه می‌کند تا مقادیر وزن‌ها را کوچک نگه دارد و از **Overfitting** جلوگیری کند.

**۱۱. سوال:** چالش اصلی هنگام استفاده از یک شبکه با تعداد پارامترهای **بسیار زیاد** چیست؟ (دو مورد)
**پاسخ:**
1.  **Overfitting (بیش‌برازش):** مدل بیش از حد به داده‌های آموزشی یاد می‌گیرد و قابلیت تعمیم (Generalization) ضعیفی خواهد داشت.
2.  **زمان و حافظه:** نیاز به زمان آموزش بسیار طولانی و سخت‌افزارهای گران‌قیمت با حافظه بالا.

**۱۲. سوال:** چگونه می‌توان برای یک مدل با پارامترهای زیاد، از لحاظ محاسباتی بهینه‌سازی انجام داد؟ (تکنیک‌های مدل فشرده‌سازی)
**پاسخ:**
1.  **Quantization (کوانتیزاسیون):** کاهش دقت پارامترها (مثلاً از ۳۲ بیت به ۱۶ یا ۸ بیت).
2.  **Pruning (هرس کردن):** حذف وزن‌های با مقدار کم (ایجاد Sparsity).
3.  **Knowledge Distillation:** آموزش یک مدل کوچک‌تر (Student) برای تقلید از خروجی‌های یک مدل بزرگ‌تر (Teacher).

**۱۳. سوال:** **Activation Function** (مانند ReLU) چه نقشی در محاسبه یک لایه دارد؟ آیا این تابع باعث افزایش تعداد پارامترها می‌شود؟
**پاسخ:** Activation Function **عدم خطی بودن (Non-Linearity)** را به شبکه معرفی می‌کند که برای یادگیری الگوهای پیچیده ضروری است. **خیر، این تابع هیچ پارامتر یادگیری جدیدی اضافه نمی‌کند.**

**۱۴. سوال:** فرض کنید در یک پروژه محدودیت حافظه (Memory Constraint) داریم. انتخاب شما بین لایه‌های FC و Conv چیست و چرا؟
**پاسخ:** در مواجهه با تصاویر بزرگ، قطعاً لایه‌های **Conv** را انتخاب می‌کنیم. به دلیل **اشتراک‌گذاری وزن** و **اتصالات محلی**، لایه‌های Conv در مقایسه با FC در ابتدای شبکه به طور قابل ملاحظه‌ای پارامترهای کمتری دارند.

**۱۵. سوال:** اگر یک لایه **Max Pooling** را به یک لایه Conv اضافه کنیم، آیا پارامترهای لایه Conv تحت تأثیر قرار می‌گیرند؟
**پاسخ:** خیر، لایه Pooling (چه Max و چه Average) **فاقد پارامترهای یادگیری** است و فقط ابعاد Feature Map خروجی را کاهش می‌دهد. بنابراین، تعداد پارامترهای لایه Conv قبل از آن ثابت می‌ماند.

---

### بخش ۴: سوالات تحلیلی و مقایسه‌ای

**۱۶. سوال:** یک لایه **Conv** با فیلتر $1 \times 1$ در نظر بگیرید. پارامترهای این فیلتر چه چیزی را محاسبه می‌کنند و چه کاربردی دارد؟
**پاسخ:** فیلتر $1 \times 1$ فقط **عمق (کانال‌ها)** را در نظر می‌گیرد. کاربرد اصلی آن **کاهش یا افزایش عمق (Dimensionality Reduction/Expansion)** خروجی است و به دلیل کوچک بودن، پارامترهای کمی مصرف می‌کند.

**۱۷. سوال:** فرمول محاسبه پارامترهای یک لایه **Batch Normalization (BN)** چیست؟ (به پارامترهای قابل آموزش BN اشاره کنید.)
**پاسخ:** لایه BN دو پارامتر قابل آموزش به ازای هر کانال ورودی دارد:
1.  **$\gamma$ (گاما):** مقیاس‌دهی (Scaling)
2.  **$\beta$ (بتا):** شیفت (Shifting)
* تعداد پارامترها: $\mathbf{2 \times \text{تعداد کانال‌های ورودی}}$

**۱۸. سوال:** اگر بخواهیم تعداد پارامترهای یک مدل را گزارش دهیم، آیا باید پارامترهای **Trainable** و **Non-Trainable** را تفکیک کنیم؟ دلیل آن چیست؟
**پاسخ:** بله، باید تفکیک کنیم. **Trainable** شامل وزن‌ها و بایاس‌ها است که آپدیت می‌شوند. **Non-Trainable** شامل پارامترهای آماری مانند میانگین متحرک (Moving Mean) و واریانس در لایه‌های BN است. این پارامترهای غیرقابل آموزش نیز حافظه مصرف می‌کنند اما در طول Backward Pass آپدیت نمی‌شوند.

**۱۹. سوال:** چگونه **Deep Learning** (یادگیری عمیق) پیچیدگی‌ها را از طریق پارامترها درک می‌کند؟
**پاسخ:** شبکه‌های عمیق از طریق **ترکیب سلسله مراتبی (Hierarchical Composition)** پارامترها کار می‌کنند. هر لایه Conv یا FC ویژگی‌های ساده‌تر لایه قبل را دریافت کرده و با استفاده از وزن‌های خود، ویژگی‌های انتزاعی‌تر و پیچیده‌تر را استخراج می‌کند.

**۲۰. سوال:** در عمل، چطور می‌توان تعیین کرد که آیا تعداد پارامترهای یک شبکه برای یک تسک کافی است یا خیر؟
**پاسخ:** باید به رابطه بین **ظرفیت مدل (Model Capacity - تعداد پارامترها)** و **پیچیدگی تسک و حجم داده** توجه کرد. اگر مدل Underfit باشد، پارامترها کم هستند و اگر Overfit باشد، پارامترها زیاد هستند. از منحنی‌های یادگیری (Learning Curves) برای تحلیل این موضوع استفاده می‌شود.




بسیار عالی، در اینجا ۲۰ سؤال و جواب تخصصی دیگر در مورد مبحث محاسبه پارامترهای شبکه‌های عصبی (FC و Conv) و موضوعات مرتبط برای آمادگی کامل در مصاحبه آورده شده است.

---

## 🧠 ۲۰ سوال تخصصی دیگر: پارامترها و پیچیدگی شبکه

### بخش ۱: لایه‌های FC و انتقال پارامتر

**۱. سوال:** اگر بخواهیم یک لایه FC با ۵۰ واحد را به یک لایه FC با ۲۰۰ واحد متصل کنیم، تعداد پارامترهای وزن و بایاس بین این دو لایه چقدر است؟
**پاسخ:**
* تعداد وزن‌ها: $\text{ورودی} \times \text{خروجی} = 50 \times 200 = \mathbf{10,000}$
* تعداد بایاس‌ها: $\text{تعداد واحدهای خروجی} = 200$
* کل پارامترها: $10,000 + 200 = \mathbf{10,200}$ پارامتر.

**۲. سوال:** فرض کنید یک شبکه عصبی کوچک دارید. اگر Batch Size را دو برابر کنید، آیا تعداد پارامترهای یادگیری شبکه تغییر می‌کند؟
**پاسخ:** خیر، **تعداد پارامترها ثابت می‌ماند.** Batch Size تنها بر **نحوه محاسبه گرادیان** و **سرعت به‌روزرسانی** پارامترها تأثیر می‌گذارد، نه بر ساختار فیزیکی (تعداد وزن‌ها و بایاس‌ها).

**۳. سوال:** اگر یک لایه FC ورودی‌ای به شکل تِنسور سه‌بعدی ($10 \times 10 \times 3$) دریافت کند، چه اتفاقی برای ورودی می‌افتد و تعداد ورودی‌های آن لایه چقدر خواهد بود؟
**پاسخ:** لایه‌های FC ورودی‌های سه‌بعدی را نمی‌پذیرند. تِنسور ورودی باید قبل از ورود به لایه FC با استفاده از عملیات **Flatten** به یک بردار یک‌بعدی تبدیل شود. بنابراین، تعداد ورودی‌ها $10 \times 10 \times 3 = \mathbf{300}$ خواهد بود. 

**۴. سوال:** آیا پارامترهای یک لایه **Dropout** نیز در محاسبه تعداد کل پارامترهای شبکه لحاظ می‌شوند؟
**پاسخ:** خیر، لایه‌های **Dropout** و **Pooling** (میانگین یا ماکسیمم) **فاقد هرگونه پارامتر یادگیری** (وزن یا بایاس) هستند و صرفاً عملیات نمونه‌برداری یا تصادفی‌سازی را انجام می‌دهند.

---

### بخش ۲: لایه‌های Convolutional (Conv) و کارایی

**۵. سوال:** یک فیلتر کانولوشن $7 \times 7$ با عمق ورودی ۱۲۸ و عمق خروجی (تعداد فیلترها) ۶۴ در نظر بگیرید. کل پارامترهای این لایه چقدر است؟
**پاسخ:**
* پارامترهای یک فیلتر: $(7 \times 7 \times \mathbf{128}) + 1 = 49 \times 128 + 1 = 6272 + 1 = 6273$
* کل پارامترها: $6273 \times \mathbf{64} = \mathbf{401,472}$ پارامتر.

**۶. سوال:** چرا لایه‌های کانولوشن از نظر پارامترها نسبت به FC در مدل‌های بینایی کارآمدتر هستند؟ (دو دلیل اصلی)
**پاسخ:**
1.  **اشتراک‌گذاری وزن (Weight Sharing):** یک مجموعه کوچک از وزن‌ها (فیلتر) در تمام نقاط تصویر استفاده می‌شود.
2.  **اتصالات محلی (Local Connectivity):** هر نورون خروجی فقط به یک ناحیه کوچک از ورودی متصل است، نه به تمام پیکسل‌ها.

**۷. سوال:** چگونه می‌توان در یک لایه Conv، تعداد پارامترهای یادگیری را بدون کاهش عمق خروجی (Feature Map) به شدت کاهش داد؟
**پاسخ:** با استفاده از فیلترهای کوچک‌تر، مانند تغییر از $5 \times 5$ به $3 \times 3$ یا استفاده از معماری‌های پیشرفته مانند **Separable Convolutions** که عملیات فضایی و عمق را جدا می‌کنند.

**۸. سوال:** آیا **Stride** بر پیچیدگی محاسباتی (زمان اجرای مدل) تأثیر دارد؟ اگر بله، چگونه؟
**پاسخ:** بله، تأثیر دارد. افزایش Stride باعث می‌شود فیلتر با گام‌های بزرگ‌تری حرکت کند، در نتیجه **ابعاد Feature Map خروجی کاهش می‌یابد** و **تعداد عملیات کانولوشن** و زمان پردازش بعدی کمتر می‌شود. 

---

### بخش ۳: پارامترها و پیچیدگی‌های عمومی

**۹. سوال:** مفهوم **Sparsity (تنک بودن)** در پارامترهای شبکه چیست و چگونه به کارایی مدل کمک می‌کند؟
**پاسخ:** Sparsity به معنی صفر بودن درصد بالایی از وزن‌های شبکه است. این امر به کاهش **حافظه مورد نیاز** برای ذخیره مدل و **کاهش بار محاسباتی** (چون ضرب در صفر حذف می‌شود) کمک می‌کند، در حالی که عملکرد مدل را حفظ می‌کند (بهینه‌سازی حافظه و سرعت).

**۱۰. سوال:** اگر در یک مدل بزرگ از **Regularization L2 (Weight Decay)** استفاده کنیم، این کار چه تأثیری بر تعداد پارامترهای مدل دارد؟
**پاسخ:** **هیچ تأثیری** بر تعداد پارامترها (وزن‌ها و بایاس‌ها) ندارد. L2 Regularization صرفاً یک **جریمه (Penalty)** به تابع زیان اضافه می‌کند تا مقادیر وزن‌ها را کوچک نگه دارد و از **Overfitting** جلوگیری کند.

**۱۱. سوال:** چالش اصلی هنگام استفاده از یک شبکه با تعداد پارامترهای **بسیار زیاد** چیست؟ (دو مورد)
**پاسخ:**
1.  **Overfitting (بیش‌برازش):** مدل بیش از حد به داده‌های آموزشی یاد می‌گیرد و قابلیت تعمیم (Generalization) ضعیفی خواهد داشت.
2.  **زمان و حافظه:** نیاز به زمان آموزش بسیار طولانی و سخت‌افزارهای گران‌قیمت با حافظه بالا.

**۱۲. سوال:** چگونه می‌توان برای یک مدل با پارامترهای زیاد، از لحاظ محاسباتی بهینه‌سازی انجام داد؟ (تکنیک‌های مدل فشرده‌سازی)
**پاسخ:**
1.  **Quantization (کوانتیزاسیون):** کاهش دقت پارامترها (مثلاً از ۳۲ بیت به ۱۶ یا ۸ بیت).
2.  **Pruning (هرس کردن):** حذف وزن‌های با مقدار کم (ایجاد Sparsity).
3.  **Knowledge Distillation:** آموزش یک مدل کوچک‌تر (Student) برای تقلید از خروجی‌های یک مدل بزرگ‌تر (Teacher).

**۱۳. سوال:** **Activation Function** (مانند ReLU) چه نقشی در محاسبه یک لایه دارد؟ آیا این تابع باعث افزایش تعداد پارامترها می‌شود؟
**پاسخ:** Activation Function **عدم خطی بودن (Non-Linearity)** را به شبکه معرفی می‌کند که برای یادگیری الگوهای پیچیده ضروری است. **خیر، این تابع هیچ پارامتر یادگیری جدیدی اضافه نمی‌کند.**

**۱۴. سوال:** فرض کنید در یک پروژه محدودیت حافظه (Memory Constraint) داریم. انتخاب شما بین لایه‌های FC و Conv چیست و چرا؟
**پاسخ:** در مواجهه با تصاویر بزرگ، قطعاً لایه‌های **Conv** را انتخاب می‌کنیم. به دلیل **اشتراک‌گذاری وزن** و **اتصالات محلی**، لایه‌های Conv در مقایسه با FC در ابتدای شبکه به طور قابل ملاحظه‌ای پارامترهای کمتری دارند.

**۱۵. سوال:** اگر یک لایه **Max Pooling** را به یک لایه Conv اضافه کنیم، آیا پارامترهای لایه Conv تحت تأثیر قرار می‌گیرند؟
**پاسخ:** خیر، لایه Pooling (چه Max و چه Average) **فاقد پارامترهای یادگیری** است و فقط ابعاد Feature Map خروجی را کاهش می‌دهد. بنابراین، تعداد پارامترهای لایه Conv قبل از آن ثابت می‌ماند.

---

### بخش ۴: سوالات تحلیلی و مقایسه‌ای

**۱۶. سوال:** یک لایه **Conv** با فیلتر $1 \times 1$ در نظر بگیرید. پارامترهای این فیلتر چه چیزی را محاسبه می‌کنند و چه کاربردی دارد؟
**پاسخ:** فیلتر $1 \times 1$ فقط **عمق (کانال‌ها)** را در نظر می‌گیرد. کاربرد اصلی آن **کاهش یا افزایش عمق (Dimensionality Reduction/Expansion)** خروجی است و به دلیل کوچک بودن، پارامترهای کمی مصرف می‌کند (مانند آنچه در معماری‌های Inception یا ResNet استفاده می‌شود).

**۱۷. سوال:** فرمول محاسبه پارامترهای یک لایه **Batch Normalization (BN)** چیست؟ (به پارامترهای قابل آموزش BN اشاره کنید.)
**پاسخ:** لایه BN دو پارامتر قابل آموزش به ازای هر کانال ورودی دارد:
1.  **$\gamma$ (گاما):** مقیاس‌دهی (Scaling)
2.  **$\beta$ (بتا):** شیفت (Shifting)
* تعداد پارامترها: $\mathbf{2 \times \text{تعداد کانال‌های ورودی}}$

**۱۸. سوال:** اگر بخواهیم تعداد پارامترهای یک مدل را گزارش دهیم، آیا باید پارامترهای **Trainable** و **Non-Trainable** را تفکیک کنیم؟ دلیل آن چیست؟
**پاسخ:** بله، باید تفکیک کنیم. **Trainable** شامل وزن‌ها و بایاس‌ها است که آپدیت می‌شوند. **Non-Trainable** شامل پارامترهای آماری مانند میانگین متحرک (Moving Mean) و واریانس در لایه‌های BN است که حافظه مصرف می‌کنند اما در طول Backward Pass آپدیت نمی‌شوند.

**۱۹. سوال:** چگونه **Deep Learning** (یادگیری عمیق) پیچیدگی‌ها را از طریق پارامترها درک می‌کند؟
**پاسخ:** شبکه‌های عمیق از طریق **ترکیب سلسله مراتبی (Hierarchical Composition)** پارامترها کار می‌کنند. هر لایه Conv یا FC ویژگی‌های ساده‌تر لایه قبل را دریافت کرده و با استفاده از وزن‌های خود، ویژگی‌های **انتزاعی‌تر و پیچیده‌تر** را استخراج و مدل‌سازی می‌کند.

**۲۰. سوال:** در عمل، چطور می‌توان تعیین کرد که آیا تعداد پارامترهای یک شبکه برای یک تسک کافی است یا خیر؟
**پاسخ:** باید به رابطه بین **ظرفیت مدل (Model Capacity - تعداد پارامترها)** و **پیچیدگی تسک و حجم داده** توجه کرد. اگر مدل Underfit باشد، پارامترها کم هستند و اگر Overfit باشد، پارامترها زیاد هستند. از منحنی‌های یادگیری (Learning Curves) برای تحلیل این موضوع استفاده می‌شود.
