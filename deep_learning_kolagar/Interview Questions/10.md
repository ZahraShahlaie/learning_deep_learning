بسیار خوب، در اینجا ۲۰ سؤال و جواب مصاحبه‌ای با تمرکز بر مفاهیم **شبکه‌های عصبی کانولوشنال (CNN)**، **عملیات کانولوشن** و **تفاوت آن با لایه‌های FC** بر اساس محتوای ارائه شده آورده شده است.

---

## 🧠 سوال و جواب‌های مصاحبه: CNN و کانولوشن

### بخش ۱: تفاوت‌های اساسی FC و CNN

**۱. سوال:** تفاوت اصلی در نحوه پردازش ورودی‌های تصویر بین لایه‌های **Fully Connected (FC)** و **Convolutional (Conv)** چیست؟
**پاسخ:** در لایه‌های **FC**، تصویر ابتدا باید **Flatten** شده و به یک بردار یک‌بعدی تبدیل شود. اما در لایه‌های **Conv**، تصویر به صورت **ماتریس سه‌بعدی (یکپارچه)** حفظ می‌شود و فیلترها مستقیماً روی آن عمل می‌کنند.

**۲. سوال:** چرا می‌گوییم لایه‌های FC **روابط فضایی (Spatial Relationships)** بین پیکسل‌ها را از دست می‌دهند، اما لایه‌های Conv آن را حفظ می‌کنند؟
**پاسخ:** وقتی تصویر Flatten می‌شود، ترتیب مکانی پیکسل‌های همسایه (مثلاً یک پیکسل در بالا و یک پیکسل در کنار) از بین می‌رود و همه به عنوان ورودی‌های جداگانه در نظر گرفته می‌شوند. اما **فیلترهای Conv** به طور **محلی** عمل می‌کنند و **همسایگی** پیکسل‌ها را در نظر می‌گیرند.

**۳. سوال:** مزیت اصلی **Conv** نسبت به **FC** در مواجهه با تصاویر بزرگ چیست؟
**پاسخ:** **کاهش شدید تعداد پارامترهای یادگیری**. در FC، پارامترها با بزرگ شدن تصویر تصاعدی افزایش می‌یابند، اما در Conv، پارامترها فقط به اندازه فیلتر و عمق بستگی دارند، نه ابعاد پیکسل‌های تصویر (به دلیل **اشتراک‌گذاری وزن**).

**۴. سوال:** مفهوم **Global View** در FC و **Local View** در Conv را توضیح دهید.
**پاسخ:** لایه‌های **FC** تصویر را به صورت **گلوبال** (کامل) می‌بینند چون هر نورون به **تمام** پیکسل‌های ورودی متصل است. در حالی که لایه‌های **Conv** به صورت **محلی** عمل می‌کنند، زیرا هر فیلتر فقط روی یک ناحیه کوچک (همان ابعاد کرنل) از ورودی عمل می‌کند.

---

### بخش ۲: عملکرد عملیات کانولوشن

**۵. سوال:** کرنل (Kernel) یا فیلتر (Filter) در لایه Conv چیست؟
**پاسخ:** **کرنل** یک **ماتریس کوچک** (معمولاً $3 \times 3$ یا $5 \times 5$) است که مقادیر آن **وزن‌های یادگیری** (Trainable Weights) شبکه هستند و وظیفه **استخراج ویژگی** را بر عهده دارد.

**۶. سوال:** عملیات کانولوشن چطور یک Feature Map تولید می‌کند؟
**پاسخ:** فیلتر روی ناحیه‌ای از تصویر قرار می‌گیرد، **ضرب نظیر به نظیر** انجام می‌دهد، نتایج را **جمع** می‌کند و عدد نهایی را در پیکسل متناظر Feature Map قرار می‌دهد. سپس فیلتر حرکت می‌کند (Sliding) و این فرآیند تکرار می‌شود. 

**۷. سوال:** چرا به خروجی لایه کانولوشن، **Feature Map (نقشه ویژگی)** می‌گویند، نه تصویر؟
**پاسخ:** زیرا ماتریس خروجی دیگر لزوماً نشان‌دهنده مقادیر بصری خام (مثل رنگ یا روشنایی) نیست، بلکه **نمایش‌دهنده ویژگی‌های استخراج شده** (مثل وجود یک لبه یا بافت) در آن ناحیه از تصویر است.

**۸. سوال:** اگر تصویر ورودی ما RGB (سه کانال رنگی) باشد، ابعاد یک فیلتر چطور تعیین می‌شود؟
**پاسخ:** عمق فیلتر (کرنل) باید **برابر با عمق ورودی** باشد. برای یک تصویر RGB، فیلتر باید $N \times N \times 3$ باشد. سه لایه فیلتر جداگانه روی سه لایه رنگی عمل می‌کنند و نتایج آن‌ها در نهایت **با هم جمع** شده و یک لایه Feature Map تولید می‌شود.

**۹. سوال:** نقش **Stride** (گام حرکت فیلتر) چیست و آیا بر تعداد پارامترها تأثیر می‌گذارد؟
**پاسخ:** Stride تعیین می‌کند که فیلتر با چه گام‌هایی روی تصویر حرکت کند. خیر، Stride بر تعداد **پارامترهای یادگیری** تأثیری ندارد، اما بر **ابعاد نهایی Feature Map خروجی** تأثیر می‌گذارد.

---

### بخش ۳: یادگیری فیلترها و مزایا

**۱۰. سوال:** در CNN، فیلترها چگونه برای استخراج ویژگی‌های مفید آموزش داده می‌شوند؟
**پاسخ:** مقادیر داخل کرنل (وزن‌ها) به جای اینکه دستی تعریف شوند، **تصادفی** مقداردهی می‌شوند و در طول آموزش با استفاده از **الگوریتم Backpropagation** به روزرسانی می‌شوند تا ویژگی‌هایی را استخراج کنند که برای وظیفه نهایی (مثل طبقه‌بندی) بهترین کارایی را دارند.

**۱۱. سوال:** منظور از **"اشتراک‌گذاری وزن" (Weight Sharing)** چیست و چه مزایایی دارد؟
**پاسخ:** به این معنی است که **یک فیلتر واحد** با همان مجموعه وزن‌ها در **تمام نقاط** تصویر استفاده می‌شود. مزایای آن عبارتند از: ۱. **کاهش شدید پارامترها** ۲. ایجاد مقاومت مدل در برابر **جابه‌جایی شیء** (Translation Invariance).

**۱۲. سوال:** اگر بخواهیم در یک لایه Conv، چندین ویژگی (مثلاً لبه عمودی و لبه افقی) را به طور همزمان استخراج کنیم، چه باید کرد؟
**پاسخ:** باید از **چندین فیلتر (کرنل)** به صورت موازی استفاده کنیم. تعداد فیلترها (عمق خروجی) تعیین می‌کند که چند نوع ویژگی مختلف استخراج شود و عمق Feature Map خروجی نیز برابر با تعداد این فیلترها خواهد بود.

**۱۳. سوال:** چگونه یک فیلتر $1 \times 1$ می‌تواند در معماری‌های پیشرفته (مانند GoogleNet یا ResNet) مفید باشد؟
**پاسخ:** فیلتر $1 \times 1$ به صورت فضایی کار نمی‌کند، بلکه فقط یک ترکیب خطی روی **کانال‌ها (عمق)** انجام می‌دهد. کاربرد اصلی آن **کاهش یا افزایش عمق (Dimensionality Reduction/Expansion)** Feature Map است، در حالی که پارامترهای بسیار کمی مصرف می‌کند.

---

### بخش ۴: محاسبه پارامترها و پیچیدگی

**۱۴. سوال:** یک لایه Conv با فیلتر $5 \times 5$، عمق ورودی ۳۲ و تعداد فیلترهای خروجی ۶۴ در نظر بگیرید. تعداد پارامترهای یادگیری این لایه چقدر است؟
**پاسخ:**
* پارامترهای یک فیلتر: $(5 \times 5 \times \text{عمق ورودی} = 32) + 1 \text{(بایاس)} = 800 + 1 = 801$
* کل پارامترها: $801 \times \text{تعداد فیلترها} = 801 \times 64 = \mathbf{51,264}$ پارامتر.

**۱۵. سوال:** چرا افزایش تعداد لایه‌های FC می‌تواند منجر به Overfitting (بیش‌برازش) شود، در حالی که افزایش لایه‌های Conv کمتر به این مشکل دامن می‌زند؟
**پاسخ:** لایه‌های FC دارای تعداد پارامترهای بسیار بالایی هستند که به مدل ظرفیت بیش از حد برای حفظ جزئیات نویز در داده‌های آموزشی می‌دهد (Overfitting). لایه‌های Conv به دلیل اشتراک‌گذاری وزن، دارای پارامترهای کمتری هستند و برای یادگیری الگوهای کلی (Feature) بهینه شده‌اند.

**۱۶. سوال:** یک لایه **Max Pooling** و یک لایه **Dropout** چند پارامتر قابل آموزش (Trainable) دارند؟
**پاسخ:** **صفر**. این لایه‌ها عملیات‌های غیرپارامتریک (بدون وزن و بایاس) هستند و فقط عملیات‌های نمونه‌برداری یا تصادفی‌سازی انجام می‌دهند.

**۱۷. سوال:** لایه **Batch Normalization (BN)** چه پارامترهایی را یاد می‌گیرد و فرمول محاسبه آن چیست؟
**پاسخ:** لایه BN دو پارامتر قابل آموزش به ازای هر کانال ورودی یاد می‌گیرد: $\gamma$ (گاما، برای مقیاس‌دهی) و $\beta$ (بتا، برای شیفت).
* فرمول: $\mathbf{2 \times \text{تعداد کانال‌های ورودی}}$

**۱۸. سوال:** توضیح دهید چرا جابه‌جا شدن ورودی‌های پیکسل در یک مدل FC لزوماً به نتیجه یکسان منجر نمی‌شود، اما در CNN این مقاومت بیشتر است؟
**پاسخ:** در FC، هر پیکسل دارای وزن‌های جداگانه است و جابه‌جایی آن‌ها یعنی تغییر در ورودی متناظر با آن وزن‌ها، که مدل آن را به عنوان یک الگوی کاملاً جدید می‌بیند. در CNN، فیلتر همان مجموعه وزن‌ها را برای همه مکان‌ها اعمال می‌کند، بنابراین الگوی جابه‌جا شده همچنان توسط فیلتر شناسایی می‌شود.

**۱۹. سوال:** چگونه CNN از طریق **سلسله مراتب لایه‌ها** ویژگی‌های پیچیده را یاد می‌گیرد؟
**پاسخ:** در لایه‌های اولیه، فیلترها ویژگی‌های ساده (مثل خطوط و لبه‌ها) را استخراج می‌کنند. در لایه‌های بعدی، فیلترها این ویژگی‌های ساده را ترکیب می‌کنند تا الگوهای پیچیده‌تر (مثل چشم گربه یا چرخ ماشین) را بسازند و در نهایت در لایه‌های انتهایی به ویژگی‌های انتزاعی می‌رسند.

**۲۰. سوال:** چه زمانی استفاده از لایه‌های **FC** در معماری‌های CNN اجتناب‌ناپذیر است؟
**پاسخ:** لایه‌های FC معمولاً در **انتهای شبکه** (پس از لایه‌های Conv و Pooling) و قبل از لایه خروجی قرار می‌گیرند تا **عملیات طبقه‌بندی (Classification)** نهایی را انجام دهند. در این مرحله، Feature Mapهای سه‌بعدی باید Flatten شده و به ورودی لایه FC برای تصمیم‌گیری نهایی داده شوند.



حتماً، در اینجا ۲۰ سؤال تخصصی دیگر در ادامه مبحث پارامترهای شبکه‌های عصبی و مقایسه FC و Conv آورده شده است.

---

## 🧠 ۲۰ سوال تخصصی تکمیلی: محاسبات پارامترها و معماری CNN

### بخش ۱: جزئیات محاسباتی لایه‌های Conv

**۱. سوال:** اگر ورودی لایه کانولوشن $28 \times 28 \times 1$ (تصویر سیاه و سفید) باشد و از ۳۲ فیلتر $3 \times 3$ استفاده کنیم، تعداد پارامترهای این لایه چقدر است؟
**پاسخ:**
* پارامترهای یک فیلتر: $(3 \times 3 \times \text{عمق ورودی}=1) + 1 \text{(بایاس)} = 10$
* کل پارامترها: $10 \times \text{تعداد فیلترها}=32 = \mathbf{320}$ پارامتر.

**۲. سوال:** فرض کنید خروجی لایه کانولوشن قبلی $16 \times 16 \times 128$ باشد. اگر در لایه بعدی از ۶۴ فیلتر $5 \times 5$ استفاده کنیم، تعداد پارامترهای لایه جدید چقدر می‌شود؟
**پاسخ:**
* پارامترهای یک فیلتر: $(5 \times 5 \times \text{عمق ورودی}=128) + 1 \text{(بایاس)} = 3200 + 1 = 3201$
* کل پارامترها: $3201 \times \text{تعداد فیلترها}=64 = \mathbf{204,864}$ پارامتر.

**۳. سوال:** اگر در یک لایه Conv از **Padding** استفاده کنیم، آیا تعداد پارامترهای یادگیری تغییر می‌کند؟
**پاسخ:** خیر، **Padding** (مانند Same یا Valid) تنها بر **ابعاد مکانی Feature Map خروجی** تأثیر می‌گذارد و هیچ وزن یا بایاس جدیدی معرفی نمی‌کند.

**۴. سوال:** چه تفاوتی بین **پارامترهای قابل آموزش (Trainable Parameters)** و **تعداد عملیات محاسباتی (FLOPS)** وجود دارد؟
**پاسخ:** پارامترهای قابل آموزش ($\mathbf{W}$ و $\mathbf{b}$) **حافظه** مدل را تعیین می‌کنند. FLOPS (عملیات ممیز شناور بر ثانیه) **سرعت و زمان اجرای** مدل را تعیین می‌کند.

### بخش ۲: تحلیل معماری و لایه‌های جانبی

**۵. سوال:** لایه **Max Pooling** چگونه به **Translation Invariance** (مقاومت در برابر جابه‌جایی) کمک می‌کند؟
**پاسخ:** با کاهش ابعاد Feature Map و نگه‌داشتن تنها بزرگترین مقدار، Pooling اطلاعات مکانی دقیق را از دست می‌دهد و صرفاً به **حضور یک ویژگی** در یک ناحیه توجه می‌کند. این باعث می‌شود جابه‌جایی جزئی شیء در ورودی، تأثیر کمتری بر خروجی داشته باشد.

**۶. سوال:** چرا معمولاً از لایه **Batch Normalization (BN)** بعد از لایه Conv یا FC و قبل از تابع فعال‌سازی استفاده می‌شود؟
**پاسخ:** BN مقادیر خروجی را نرمالایز می‌کند تا میانگین نزدیک به صفر و واریانس نزدیک به یک داشته باشد. این کار باعث می‌شود **آموزش سریع‌تر** شود و مشکل **Covariate Shift داخلی** (تغییر توزیع ورودی‌های لایه‌ها) کاهش یابد و به **بهبود پایداری** مدل کمک کند.

**۷. سوال:** چرا لایه **Flatten** معمولاً به عنوان یک **گردن بطری (Bottleneck)** از نظر تعداد پارامترها شناخته می‌شود؟
**پاسخ:** لایه Flatten یک تِنسور سه‌بعدی بزرگ (حاصل از Conv و Pooling) را به یک بردار یک‌بعدی تبدیل می‌کند. اگر این بردار ورودی لایه FC بعدی شود، تعداد پارامترهای لایه FC به شدت بالا می‌رود و بار محاسباتی سنگینی ایجاد می‌کند.

**۸. سوال:** اگر یک لایه **FC** با ۵۰۰ نورون را مستقیماً به یک لایه **Conv** متصل کنیم، ابعاد ورودی لایه Conv چگونه خواهد بود؟
**پاسخ:** اتصال مستقیم FC به Conv معمول نیست. اما اگر اتفاق بیفتد، خروجی FC (۵۰۰) باید به یک تِنسور سه‌بعدی **reshape** شود (مثلاً $10 \times 10 \times 5$ یا $25 \times 4 \times 5$) تا برای فیلترهای Conv قابل استفاده باشد.

### بخش ۳: مقایسه‌های تخصصی

**۹. سوال:** مهم‌ترین تفاوت ساختاری در نحوه استفاده از وزن‌ها بین لایه‌های FC و Conv چیست؟
**پاسخ:**
* **Conv:** از **اشتراک‌گذاری وزن (Weight Sharing)** استفاده می‌کند (یک کرنل برای کل ورودی).
* **FC:** از **وزن‌های اختصاصی (Dedicated Weights)** استفاده می‌کند (هر ورودی به هر نورون، وزن مخصوص خود را دارد).

**۱۰. سوال:** اگر بخواهیم یک مدل را از نظر **حجم فایل (Model Size)** بهینه کنیم، آیا به تعداد پارامترهای Trainable توجه می‌کنیم یا FLOPS؟
**پاسخ:** به تعداد **پارامترهای Trainable** (و Non-Trainable). حجم مدل ذخیره شده مستقیماً به تعداد و دقت (مثلاً ۳۲ بیتی بودن) وزن‌ها و بایاس‌ها بستگی دارد.

**۱۱. سوال:** چرا فیلترهای Conv لایه‌های اولیه (نزدیک به ورودی) معمولاً یاد می‌گیرند که لبه‌ها و خطوط را استخراج کنند؟
**پاسخ:** این ویژگی‌ها **محلی‌ترین و ساده‌ترین** الگوها در تصویر هستند. فیلترها در لایه‌های اولیه برای به حداقل رساندن خطا، ابتدا باید این الگوهای پایه‌ای را یاد بگیرند و بعداً در لایه‌های عمیق‌تر آن‌ها را ترکیب کنند.

**۱۲. سوال:** چگونه می‌توان با استفاده از لایه‌های **Conv**، به جای لایه‌های **FC**، عملیات طبقه‌بندی (Classification) را انجام داد؟ (تکنیک Global Average Pooling)
**پاسخ:** می‌توان قبل از لایه نهایی خروجی (Softmax)، به جای Flatten و FC، از **Global Average Pooling (GAP)** استفاده کرد که میانگین هر Feature Map را می‌گیرد. این کار منجر به **حذف پارامترهای سنگین FC** می‌شود و مقاومت مدل در برابر Overfitting را افزایش می‌دهد.

### بخش ۴: ملاحظات پیچیدگی و بهینه‌سازی

**۱۳. سوال:** **Quantization (کوانتیزاسیون)** به عنوان یک تکنیک بهینه‌سازی، چه تأثیری بر پارامترهای مدل دارد؟
**پاسخ:** Quantization تعداد پارامترها را تغییر نمی‌دهد، اما **حجم حافظه مورد نیاز** برای ذخیره آن‌ها را کاهش می‌دهد، زیرا دقت آن‌ها را از فلوت ۳۲ بیتی به دقت کمتر (مانند ۸ بیت) تغییر می‌دهد.

**۱۴. سوال:** در زمان ساخت یک مدل برای **دستگاه‌های کم‌توان (Edge Devices)**، تمرکز اصلی شما روی کاهش چه عاملی است؟ (پارامترها یا FLOPS)
**پاسخ:** تمرکز باید روی کاهش **هر دو** باشد. کاهش **پارامترها** برای **کاهش حجم حافظه** مورد نیاز است و کاهش **FLOPS** برای **کاهش زمان پاسخ‌دهی (Latency)** و مصرف انرژی.

**۱۵. سوال:** اگر یک لایه Conv با ۱۰۰ فیلتر $3 \times 3$ و عمق ۱۲۸ داشته باشیم، تعداد پارامترهای آن چقدر است؟
**پاسخ:**
* پارامترهای یک فیلتر: $(3 \times 3 \times 128) + 1 = 1152 + 1 = 1153$
* کل پارامترها: $1153 \times 100 = \mathbf{115,300}$ پارامتر.

**۱۶. سوال:** آیا **تابع فعال‌سازی (Activation Function)** مانند ReLU بر پیچیدگی محاسباتی (FLOPS) تأثیر می‌گذارد؟
**پاسخ:** بله، هرچند بسیار ناچیز. این توابع نیازمند عملیات محاسباتی (هرچند ساده) هستند که به مجموع FLOPS مدل اضافه می‌شوند، اما تأثیری بر تعداد پارامترهای یادگیری ندارند.

**۱۷. سوال:** چرا استفاده از فیلترهای بزرگ‌تر (مثلاً $7 \times 7$) در شبکه‌های مدرن کمتر رایج است و چه جایگزینی دارد؟
**پاسخ:** فیلترهای بزرگ‌تر پارامترهای بسیار بیشتری دارند و از نظر محاسباتی گران‌ترند. جایگزین رایج، **استفاده از چندین فیلتر کوچک $3 \times 3$ به صورت پشت سر هم** است که می‌تواند همان میدان دید مؤثر (Receptive Field) را پوشش دهد اما با پارامترهای بسیار کمتر.

**۱۸. سوال:** منظور از **Receptive Field (میدان دید مؤثر)** در CNN چیست؟
**پاسخ:** Receptive Field به اندازه‌ی ناحیه‌ای از **تصویر ورودی اولیه** اشاره دارد که بر خروجی یک نورون در یک لایه خاص تأثیر می‌گذارد. با عمیق‌تر شدن شبکه و استفاده از Conv و Pooling، Receptive Field نورون‌ها **بزرگ‌تر** می‌شود.

**۱۹. سوال:** چگونه **Padding** به حفظ ابعاد Feature Map و جلوگیری از کاهش اطلاعات کمک می‌کند؟
**پاسخ:** Padding (به ویژه Zero Padding) به اطراف تصویر ورودی مقادیر صفر اضافه می‌کند. این کار تضمین می‌کند که فیلترها بتوانند **حاشیه‌های تصویر** را نیز پردازش کنند و از کوچک شدن بیش از حد ابعاد Feature Map در لایه‌های متوالی جلوگیری می‌کند.

**۲۰. سوال:** چه پارامترهایی در لایه‌های **Batch Normalization** به عنوان **Non-Trainable (غیرقابل آموزش)** ذخیره می‌شوند؟
**پاسخ:** **میانگین متحرک (Moving Mean)** و **واریانس متحرک (Moving Variance)**. این مقادیر در طول آموزش برای فاز استنتاج (Inference) ذخیره می‌شوند و در طول Backpropagation آپدیت نمی‌شوند.
