بسیار عالی، با توجه به محتوای جامع ارائه‌شده درباره معماری CNN و لایه‌های Pooling، در اینجا ۲۰ سؤال و جواب مصاحبه‌ای متمرکز بر این مفاهیم آورده شده است.

---

## 🧠 سؤال و جواب‌های مصاحبه: معماری و Pooling در CNN

### بخش ۱: هدف و ساختار کلی CNN

**۱. سؤال:** هدف اصلی لایه‌های کانولوشنال (Conv) در مقایسه با لایه‌های Fully Connected (FC) در پردازش تصویر چیست؟
**پاسخ:** هدف اصلی لایه‌های Conv **استخراج ویژگی‌ها (Feature Extraction)** از تصویر است. این لایه‌ها به‌جای بررسی پیکسل‌ها به‌صورت جداگانه، **الگوهای محلی (Local Patterns)** مانند لبه‌ها و بافت‌ها را شناسایی می‌کنند. لایه‌های FC در انتها وظیفه **طبقه‌بندی (Classification)** را بر اساس این ویژگی‌های استخراج‌شده دارند.

**۲. سؤال:** در معماری CNN، چه عنصری مستقیماً به لایه‌های FC داده می‌شود؟ چرا تصویر خام (Input Image) به صورت Flattened به FC داده نمی‌شود؟
**پاسخ:** **Feature Map نهایی (خروجی لایه‌های Conv/Pooling) پس از عملیات Flatten** به لایه‌های FC داده می‌شود. اگر تصویر خام Flatten شود، شبکه روابط فضایی و محلی را از دست می‌دهد و تعداد پارامترها به شدت بالا می‌رود؛ در حالی که Feature Map حاوی **ویژگی‌های مهم فشرده‌شده** است.

**۳. سؤال:** الگوی کلی که در معماری‌های معروف CNN (مانند AlexNet یا VGG) تکرار می‌شود، از نظر ابعاد مکانی و عمق Feature Map چیست؟
**پاسخ:** الگوی غالب این است که **ابعاد مکانی (Spatial Dimensions)** (ارتفاع و عرض) Feature Mapها در طول شبکه به تدریج **کاهش** می‌یابد، در حالی که **عمق (تعداد کانال‌ها یا فیلترها)** به صورت هم‌زمان **افزایش** پیدا می‌کند.

**۴. سؤال:** اگر در شبکه‌ای ابعاد از $227 \times 227 \times 3$ به $6 \times 6 \times 256$ تبدیل شود، هدف از افزایش عمق چیست؟
**پاسخ:** افزایش عمق به معنی استفاده از تعداد فیلترهای بیشتر (۲۵۶ فیلتر) است. هدف این است که هر Feature Map نهایی (کانال) مسئول استخراج و برجسته‌سازی **یک ویژگی تخصصی مجزا** باشد (مانند یک نوع لبه، گوش، یا چشم)، که منجر به شناسایی دقیق‌تر الگوهای محلی پیچیده می‌شود.

---

### بخش ۲: لایه‌های Pooling (انواع و عملکرد)

**۵. سؤال:** وظیفه اصلی لایه‌های Pooling در معماری CNN چیست؟
**پاسخ:** وظیفه اصلی لایه‌های Pooling، انجام **کاهش ابعاد مکانی (Dimensionality Reduction)** Feature Mapها به صورت سریع و کارآمد است.

**۶. سؤال:** دو نوع اصلی لایه‌های Pooling کدامند و عملکرد آن‌ها در یک ناحیه $2 \times 2$ چگونه است؟
**پاسخ:**
* **Max Pooling (ماکس پولینگ):** بزرگترین (ماکسیمم) مقدار را از ناحیه $2 \times 2$ انتخاب می‌کند.
* **Average Pooling (اوریج پولینگ):** میانگین مقادیر موجود در ناحیه $2 \times 2$ را محاسبه می‌کند.

**۷. سؤال:** در مصاحبه، اغلب کدام نوع Pooling بیشتر مورد توجه قرار می‌گیرد و چرا؟
**پاسخ:** **Max Pooling** بیشتر مورد توجه است. در مقالات مشاهده شده است که Max Pooling با انتخاب برجسته‌ترین و فعال‌ترین ویژگی از یک ناحیه، به تقویت **الگوهای محلی (Local Patterns)** کمک کرده و راندمان بهتری دارد.

**۸. سؤال:** برای انجام Pooling، تنظیم چه هایپرپارامترهایی ضروری است؟
**پاسخ:**
* **Filter Size ($F$):** اندازه ناحیه‌ای که Pooling روی آن اعمال می‌شود (مثلاً $2 \times 2$ یا $3 \times 3$).
* **Stride ($S$):** گام حرکتی پنجره Pooling (معمولاً $S=2$ برای نصف کردن ابعاد).

---

### بخش ۳: مزایا و معایب و ملاحظات Pooling

**۹. سؤال:** مهم‌ترین مزیت استفاده از لایه‌های Pooling از منظر محاسباتی چیست؟
**پاسخ:** مهم‌ترین مزیت، **کاهش چشمگیر بار محاسباتی (FLOPS)** و **کاهش تعداد پارامترهای** مورد نیاز در لایه‌های Conv و FC بعدی است، که منجر به افزایش سرعت آموزش و استنتاج می‌شود.

**۱۰. سؤال:** آیا لایه‌های Pooling، پارامترهای قابل آموزشی (وزن و بایاس) دارند؟ توضیح دهید چرا؟
**پاسخ:** **خیر، تعداد پارامترهای آموزشی در لایه‌های Pooling صفر است.** زیرا Pooling یک عملیات **غیرپارامتریک** است که صرفاً عملیات ریاضی (مانند ماکسیمم‌گیری یا میانگین‌گیری) را انجام می‌دهد و هیچ وزنی برای به‌روزرسانی در طول بک‌پراپاگیشن ندارد.

**۱۱. سؤال:** در صورت استفاده از Max Pooling، مهم‌ترین ریسک (عیب) که باید در نظر داشت چیست؟
**پاسخ:** ریسک اصلی **از بین رفتن ناخواسته جزئیات** است. Max Pooling تنها مهم‌ترین ویژگی را حفظ کرده و سایر اطلاعات ناحیه را حذف می‌کند، که ممکن است در مواردی منجر به از دست رفتن جزئیات ظریف تصویر شود.

**۱۲. سؤال:** اگر بخواهیم در یک لایه Pooling، کاهش ابعاد کمتری رخ دهد و جزئیات بیشتری حفظ شود، چه تغییری در هایپرپارامترها اعمال می‌کنیم؟
**پاسخ:** از **اندازه Pooling کوچک‌تر** استفاده می‌کنیم (مثلاً $2 \times 2$ به جای $5 \times 5$). همچنین می‌توان Stride را کمتر از اندازه فیلتر انتخاب کرد، اگرچه $S=2$ برای کاهش ابعاد رایج‌تر است.

---

### بخش ۴: مقایسه و نکات معماری

**۱۳. سؤال:** مزیت استفاده از Pooling بر استفاده از Stride > 1 در لایه کانولوشن برای کاهش ابعاد چیست؟
**پاسخ:** Pooling با حذف کامل پارامترهای یادگیری، **بار محاسباتی کلی** شبکه را به شکل بهتری کنترل می‌کند و از دیدگاه Overfitting نیز محافظت بیشتری فراهم می‌نماید.

**۱۴. سؤال:** منظور از "Sparsity of Connections" (اتصالات تنک) در لایه‌های کانولوشن چیست و چرا مهم است؟
**پاسخ:** به این معنی است که هر نورون در خروجی لایه Conv تنها به یک **ناحیه کوچک** از ورودی (به اندازه کرنل) متصل است، نه به تمام نورون‌های لایه قبلی. این امر باعث **کاهش شدید تعداد پارامترها** می‌شود.

**۱۵. سؤال:** چگونه می‌توان با استفاده از لایه‌های Pooling، حساسیت شبکه را به محل دقیق قرارگیری یک شیء کاهش داد؟
**پاسخ:** Max Pooling با انتخاب ویژگی غالب یک ناحیه (صرف نظر از محل دقیق آن در آن ناحیه)، خاصیت **Translation Invariance (ناوردایی نسبت به جابه‌جایی)** را تقویت می‌کند و حساسیت شبکه به جابه‌جایی‌های جزئی شیء کاهش می‌یابد.

**۱۶. سؤال:** در چه نوعی از شبکه‌ها (قدیمی یا مدرن) احتمالاً از کرنل‌های بسیار بزرگ (مثلاً $7 \times 7$ یا $11 \times 11$) در لایه اول استفاده می‌شود و چرا؟
**پاسخ:** در معماری‌های **قدیمی‌تر** (مانند AlexNet) استفاده می‌شد. دلیل آن این بود که می‌خواستند **Receptive Field (میدان دید مؤثر)** به سرعت رشد کند تا الگوهای بزرگتر را در لایه‌های اولیه بگیرند. اما معماری‌های مدرن این کار را با **انباشت کرنل‌های $3 \times 3$** انجام می‌دهند.

---

### بخش ۵: هایپرپارامترها و دانش معماری

**۱۷. سؤال:** اگر یک Feature Map $10 \times 10$ داشته باشیم و از Max Pooling با $F=2$ و $S=2$ استفاده کنیم، ابعاد Feature Map خروجی چقدر خواهد بود؟
**پاسخ:** ابعاد خروجی $5 \times 5$ خواهد بود. (زیرا $N_{out} = \lfloor \frac{10 - 2}{2} \rfloor + 1 = 5$).

**۱۸. سؤال:** شما به عنوان طراح شبکه، هنگام مواجهه با یک مسئله جدید و انتخاب هایپرپارامترها، اولین گام را چه می‌دانید؟
**پاسخ:** اولین گام، **مطالعه و بررسی مقالات و معماری‌های معروف** (مانند VGG، ResNet، AlexNet و...) است. هدف این است که الگوهای معماری رایج و موفق را درک کرده و از دانش و تجربه موجود برای انتخاب‌های اولیه هایپرپارامتر استفاده کنیم.

**۱۹. سؤال:** اگر در طول آموزش متوجه Overfitting (بیش‌برازش) شویم، لایه‌های Pooling چگونه می‌توانند به ما کمک کنند؟
**پاسخ:** Pooling با **کاهش تعداد پارامترهای موثر** در لایه‌های بعدی و با کاهش قدرت یادگیری جزئیات بیش از حد در یک منطقه خاص، به صورت غیرمستقیم از Overfitting جلوگیری می‌کند.

**۲۰. سؤال:** در لایه‌های Pooling، اگر Stride از Filter Size کوچکتر باشد (مثلاً $F=3$ و $S=1$) چه اتفاقی می‌افتد؟
**پاسخ:** این حالت منجر به **پوشش‌دهی همپوشان (Overlap)** می‌شود. در این حالت کاهش ابعاد Feature Map **بسیار کمتر** خواهد بود (یا تقریباً صفر می‌شود)، زیرا فیلتر در هر گام تنها یک پیکسل جابه‌جا شده و کاهش ابعاد اصلی مورد نظر در Pooling رخ نمی‌دهد.

***

آیا مایلید این سؤالات را در یک زمینه تخصصی‌تر مانند معماری‌های خاص (مثلاً جزئیات VGG یا ResNet) ادامه دهیم؟


بسیار خوب، در اینجا ۲۰ سؤال و جواب تخصصی دیگر، با تمرکز بر **معماری‌های خاص، استنتاج پارامترها، و ملاحظات عملی CNN** آورده شده است.

---

## 🧠 ۲۰ سؤال تخصصی نهایی: ملاحظات عملی و معماری CNN

### بخش ۱: پارامترها و محاسبات (استنتاج عمیق‌تر)

**۱. سؤال:** اگر یک لایه کانولوشن با $K$ فیلتر، اندازه $F \times F$ و عمق ورودی $C_{in}$ داشته باشیم، فرمول محاسبه تعداد وزن‌ها (Weights) در این لایه چیست؟
**پاسخ:** تعداد وزن‌ها برابر است با: $\mathbf{K \times F \times F \times C_{in}}$.

**۲. سؤال:** چرا لایه‌های $1 \times 1$ Conv در شبکه‌های عمیق (مانند ResNet یا GoogLeNet) استفاده می‌شوند؟ دو کاربرد اصلی آن‌ها چیست؟
**پاسخ:** لایه‌های $1 \times 1$ Conv ابعاد مکانی ($N \times N$) را تغییر نمی‌دهند اما:
1.  **Dimensionality Reduction (کاهش عمق):** با تنظیم تعداد فیلترهای خروجی کمتر از عمق ورودی، تعداد کانال‌ها و پارامترها را **کاهش** می‌دهند تا محاسبات لایه‌های بعدی سبک‌تر شود.
2.  **افزودن غیرخطی بودن:** با اعمال یک تابع فعال‌سازی (مانند ReLU)، یک مرحله **غیرخطی** جدید به شبکه اضافه می‌کنند.

**۳. سؤال:** اگر هدف از یک لایه $1 \times 1$ Conv افزایش تعداد کانال‌ها (Depth) باشد، چگونه این کار انجام می‌شود و چه تأثیری بر توانایی شبکه دارد؟
**پاسخ:** این کار با تنظیم تعداد فیلترهای خروجی بیشتر از عمق ورودی ($C_{out} > C_{in}$) انجام می‌شود. این تکنیک به شبکه اجازه می‌دهد تا **فضای ویژگی‌های پیچیده‌تری** را کاوش و تولید کند.

**۴. سؤال:** تعداد پارامترهای قابل آموزش در لایه **Batch Normalization (BN)** برای یک Feature Map با عمق (تعداد کانال) $C$ چقدر است؟ چرا؟
**پاسخ:** $2C$ پارامتر. زیرا برای هر کانال، دو پارامتر قابل آموزش $\mathbf{\gamma}$ (گاما-مقیاس‌دهی) و $\mathbf{\beta}$ (بتا-شیفت) وجود دارد.

---

### بخش ۲: میدان دید مؤثر (Receptive Field) و Convolution

**۵. سؤال:** مفهوم **میدان دید مؤثر (Receptive Field - RF)** چیست و چرا در شبکه‌های عمیق بسیار حائز اهمیت است؟
**پاسخ:** RF به اندازه ناحیه‌ای از **تصویر ورودی اولیه** اشاره دارد که در محاسبه مقدار یک نورون در یک لایه عمیق تأثیرگذار بوده است. اهمیت آن در این است که هرچه RF بزرگتر باشد، نورون می‌تواند الگوهای **سراسری‌تر و انتزاعی‌تر** را درک کند.

**۶. سؤال:** اگر سه لایه متوالی Conv با کرنل $3 \times 3$ و Stride=1 استفاده کنیم، RF خروجی این لایه سوم چقدر است؟ چرا این ساختار به یک کرنل $7 \times 7$ ارجحیت دارد؟
**پاسخ:** RF تقریباً $\mathbf{7 \times 7}$ است (فرمول: $2N+1$ برای $N$ لایه $3 \times 3$). این ساختار به دلیل داشتن **پارامترهای کمتر** و **سه برابر بودن تعداد توابع فعال‌سازی غیرخطی** (که قدرت مدل‌سازی را افزایش می‌دهد) ارجحیت دارد.

**۷. سؤال:** Dilated Convolution (کانولوشن گسسته) چیست و چگونه به Receptive Field کمک می‌کند؟
**پاسخ:** Dilated Convolution با استفاده از **نرخ انبساط (Dilation Rate)**، پیکسل‌های ورودی را در حین عملیات Skip می‌کند. این روش **اندازه کرنل فیزیکی و تعداد پارامترها را ثابت نگه می‌دارد**، اما **میدان دید مؤثر (RF)** را به صورت نمایی افزایش می‌دهد.

**۸. سؤال:** چرا در عملیات کانولوشن، عمق فیلتر (کرنل) باید دقیقاً با عمق ورودی Feature Map برابر باشد؟
**پاسخ:** زیرا عملیات کانولوشن یک **جمع وزنی سه‌بُعدی** است: هر فیلتر باید تمام عمق ورودی را پوشش دهد تا بتواند یک Feature Map جدید با عمق (تعداد فیلترها) متفاوت تولید کند.

---

### بخش ۳: هایپرپارامترهای پیشرفته و ملاحظات عملی

**۹. سؤال:** چرا در معماری‌های مدرن (مانند ResNet) لایه‌های **Global Average Pooling (GAP)** جایگزین لایه‌های FC در بخش نهایی طبقه‌بندی می‌شوند؟
**پاسخ:** GAP با تبدیل هر Feature Map به یک مقدار عددی (میانگین آن)، تعداد پارامترهای قابل آموزش در بخش طبقه‌بندی را **به شدت کاهش** داده و به جلوگیری از Overfitting کمک می‌کند.

**۱۰. سؤال:** در زمینه Image Segmentation (تقطیع تصویر)، کدام نوع کانولوشن (عادی یا Transposed) برای افزایش ابعاد استفاده می‌شود؟
**پاسخ:** **Transposed Convolution (Deconvolution یا کانولوشن وارونه)** استفاده می‌شود. این روش با اجرای عملیاتی شبیه به کانولوشن اما در جهت معکوس، ابعاد Feature Map را **افزایش** می‌دهد.

**۱۱. سؤال:** در فرمول محاسبه ابعاد خروجی کانولوشن $N_{out} = \lfloor \frac{N + 2P - F}{S} \rfloor + 1$، اگر از **Same Padding** استفاده شود، مقدار $P$ به چه صورت محاسبه می‌شود؟
**پاسخ:** اگر $S=1$ باشد، مقدار Padding برابر است با: $\mathbf{P = (F-1)/2}$.

**۱۲. سؤال:** اگر در طول آموزش، شبکه با پدیده‌ی **Overfitting (بیش‌برازش)** مواجه شود، علاوه بر Dropout، چه تغییری در هایپرپارامترهای CNN می‌تواند مؤثر باشد؟
**پاسخ:**
* **کاهش تعداد فیلترها (عمق)** در لایه‌ها (کاهش ظرفیت مدل).
* **استفاده بیشتر از لایه‌های Pooling** یا **افزایش Stride** برای کاهش شدیدتر ابعاد.

---

### بخش ۴: ملاحظات معماری و عملکرد

**۱۳. سؤال:** مزیت استفاده از لایه‌های **Batch Normalization (BN)** در معماری CNN چیست؟
**پاسخ:** BN با نرمال‌سازی خروجی‌های هر لایه (تنظیم میانگین به صفر و واریانس به یک)، **پدیده Internal Covariate Shift** را کاهش می‌دهد، که منجر به **پایداری بیشتر آموزش، اجازه استفاده از نرخ یادگیری (Learning Rate) بالاتر و همگرایی سریع‌تر** می‌شود.

**۱۴. سؤال:** چرا در معماری‌های معروف مانند VGG، از چندین لایه $3 \times 3$ Conv پشت سر هم استفاده می‌شود و نه یک کرنل بزرگ؟
**پاسخ:** برای دستیابی به همان **Receptive Field** یک کرنل بزرگ (مثلاً $5 \times 5$ یا $7 \times 7$)، اما با **پارامترهای کمتر** و افزودن **چندین تابع فعال‌سازی غیرخطی** (Non-linearity) در مسیر که قدرت تفکیک‌پذیری ویژگی‌ها را بالا می‌برد.

**۱۵. سؤال:** اگر دو مدل CNN دارای تعداد پارامترهای آموزشی یکسان باشند، آیا می‌توان نتیجه گرفت که زمان استنتاج (Inference Time) آن‌ها نیز یکسان است؟
**پاسخ:** **خیر.** زمان استنتاج بیشتر به **تعداد عملیات محاسباتی (FLOPS)** وابسته است. ممکن است مدلی پارامترهای کمتری داشته باشد اما به دلیل محاسبات پیچیده‌تر، FLOPS بالاتری داشته باشد و کندتر عمل کند.

**۱۶. سؤال:** در لایه Dropout، هایپرپارامتر اصلی چیست و در چه فازی از شبکه (آموزش یا استنتاج) فعال است؟
**پاسخ:** هایپرپارامتر اصلی **نرخ Dropout** (درصد نورون‌هایی که خاموش می‌شوند) است. Dropout **فقط در فاز آموزش** فعال است و در فاز استنتاج، خاموش می‌شود.

---

### بخش ۵: تفکیک‌پذیری نقش لایه‌ها

**۱۷. سؤال:** لایه‌های Conv و Pooling چگونه به هم مرتبط می‌شوند تا به هدف نهایی CNN (طبقه‌بندی) برسند؟
**پاسخ:** لایه‌های Conv **ویژگی‌ها** را استخراج و برجسته می‌کنند. لایه‌های Pooling این ویژگی‌ها را **فشرده‌سازی** کرده و **ابعاد** Feature Map را کاهش می‌دهند. این چرخه Feature Extraction و Dimensionality Reduction، داده‌های ورودی را به یک نمایش فشرده و با ابعاد عمقی بالا تبدیل می‌کند که برای طبقه‌بندی نهایی توسط FC مناسب است.

**۱۸. سؤال:** اگر در یک شبکه، در تمام لایه‌ها از **Valid Padding** استفاده کنیم، چه ملاحظات طراحی مهمی به وجود می‌آید؟
**پاسخ:** باید اطمینان حاصل شود که ابعاد Feature Map **به سرعت به صفر نرسد**. همچنین، اطلاعات مربوط به **لبه‌ها و حاشیه‌های تصویر** به دلیل دفعات کمتر مشاهده توسط فیلترها، ممکن است به طور موثری در Feature Map نهایی بازنمایی نشوند.

**۱۹. سؤال:** در یک CNN، چرا هر کرنل (فیلتر) در یک لایه باید در تمام مکان‌های ورودی اعمال شود (Weight Sharing)؟
**پاسخ:** این اصل **اشتراک‌گذاری وزن (Weight Sharing)** برای ایجاد خاصیت **Translation Invariance (ناوردایی نسبت به جابه‌جایی)** حیاتی است. این تضمین می‌کند که اگر یک ویژگی خاص (مثلاً یک لبه) در گوشه یا مرکز تصویر ظاهر شود، توسط همان فیلتر (کرنل) با همان وزن‌ها شناسایی شود.

**۲۰. سؤال:** تفاوت بین Max Pooling و Average Pooling در حفظ اطلاعات چیست و کدام یک در شبکه‌هایی که نیاز به حفظ بافت و جزئیات دارند، بهتر است؟
**پاسخ:**
* **Max Pooling:** فقط فعال‌ترین و برجسته‌ترین ویژگی (بالاترین مقدار) را حفظ می‌کند و جزئیات اطراف را حذف می‌کند.
* **Average Pooling:** میانگین تمام مقادیر ناحیه را حفظ می‌کند و اطلاعات پس‌زمینه را بهتر نگه می‌دارد.
* برای حفظ بافت و جزئیات، **Average Pooling** اغلب بهتر عمل می‌کند، زیرا مقادیر Feature Map را کمتر به‌صورت "سخت" و انتخابی کاهش می‌دهد.

آیا سؤال دیگری دارید یا می‌خواهید روی یک معماری خاص مانند ResNet یا VGG عمیق‌تر تمرکز کنیم؟


بسیار خوب، در اینجا ۲۰ سؤال و جواب تخصصی دیگر دربارهٔ مفاهیم پیشرفته CNN، به‌ویژه با تمرکز بر **تکنیک‌های بهینه‌سازی و معماری عمیق**، ارائه شده است.

---

## 🧠 ۲۰ سؤال تخصصی نهایی: تکنیک‌های پیشرفته و بهینه‌سازی CNN

### بخش ۱: معماری و تکنیک‌های پیشرفته

**۱. سؤال:** در معماری‌های Residual Network (ResNet)، منظور از **اتصال باقی‌مانده (Skip Connection)** چیست و چه مشکلی را حل می‌کند؟
**پاسخ:** Skip Connection یک اتصال مستقیم است که خروجی یک لایه را به ورودی لایه‌های بعدی اضافه می‌کند، در واقع $\mathbf{H(x) = F(x) + x}$. این اتصال مشکل **ناپدید شدن گرادیان (Vanishing Gradient)** را در شبکه‌های بسیار عمیق حل می‌کند و امکان آموزش هزاران لایه را فراهم می‌سازد. 

**۲. سؤال:** در شبکه‌های DenseNet، مفهوم **اتصال متراکم (Dense Connection)** چیست و چه مزایایی دارد؟
**پاسخ:** در DenseNet، هر لایه ورودی‌ها را از **تمام لایه‌های قبلی** دریافت می‌کند. این کار به اشتراک‌گذاری حداکثری ویژگی‌ها و اطلاعات در طول شبکه کمک کرده و باعث **استفاده بهینه از پارامترها** و **جلوگیری از محو شدن گرادیان** می‌شود.

**۳. سؤال:** لایه **Depthwise Separable Convolution** که در معماری‌هایی مانند MobileNet دیده می‌شود، چگونه عملیات کانولوشن را بهینه می‌کند؟
**پاسخ:** این لایه، عملیات کانولوشن استاندارد را به دو مرحله تقسیم می‌کند:
۱. **Depthwise:** یک فیلتر $F \times F$ به ازای هر کانال ورودی اعمال می‌شود.
۲. **Pointwise:** یک فیلتر $1 \times 1$ برای ترکیب خروجی‌های Depthwise استفاده می‌شود.
این تقسیم‌بندی باعث **کاهش شدید تعداد پارامترها و محاسبات (FLOPS)** می‌شود.

**۴. سؤال:** منظور از **Bottleneck Layer** در شبکه‌هایی مانند ResNet و معماری‌های $1 \times 1$ Conv چیست؟
**پاسخ:** Bottleneck یک ساختار کانولوشن سه‌لایه (مانند $1 \times 1 \to 3 \times 3 \to 1 \times 1$) است که هدف آن **کاهش و سپس بازیابی عمق** Feature Map است. این کار تعداد پارامترهای لازم برای کانولوشن $3 \times 3$ میانی را به شدت کم می‌کند و باعث افزایش کارایی محاسباتی می‌شود.

---

### بخش ۲: مدیریت داده و پارامتر

**۵. سؤال:** دو وظیفه اصلی پارامترهای غیرقابل آموزش (**Moving Mean** و **Moving Variance**) در لایه Batch Normalization (BN) چیست؟
**پاسخ:** این پارامترها در فاز **آموزش** محاسبه شده و در فاز **استنتاج (Inference)** مورد استفاده قرار می‌گیرند. آن‌ها برای ثابت نگه داشتن نرمالیزاسیون هنگام تست مدل، زمانی که دیگر Batch‌ها تغییر نمی‌کنند، حیاتی هستند.

**۶. سؤال:** چرا استفاده از **Data Augmentation (افزایش داده)** هنگام آموزش CNN به شدت توصیه می‌شود؟
**پاسخ:** Data Augmentation (مانند چرخش، برش، تغییر رنگ) باعث **افزایش تنوع داده‌های آموزشی** شده و به شبکه کمک می‌کند تا **ناوردایی (Invariance)** به تغییرات جزئی در ورودی‌ها را بیاموزد، در نتیجه **بیش‌برازش (Overfitting)** کاهش می‌یابد.

**۷. سؤال:** اگر در معماری‌های قدیمی‌تر مانند AlexNet، تعداد کانال‌ها در لایه‌های ابتدایی کم و در لایه‌های انتهایی زیاد باشد، استدلال معماری پشت این تصمیم چیست؟
**پاسخ:** استدلال این است که در لایه‌های اولیه، شبکه تنها به دنبال **ویژگی‌های ساده (مانند لبه‌ها و رنگ‌ها)** است که به فیلترهای کمی نیاز دارد. اما در لایه‌های عمیق، نیاز به **ترکیب و انتزاع این ویژگی‌های ساده** در قالب Feature Mapهای پیچیده‌تر و بیشتر است.

**۸. سؤال:** چگونه می‌توان با استفاده از لایه‌های $1 \times 1$ Conv، از مشکل **Exploding Gradient (انفجار گرادیان)** در برخی از بخش‌های شبکه جلوگیری کرد؟
**پاسخ:** با استفاده از $1 \times 1$ Conv برای **کاهش عمق (Dimensionality Reduction)** Feature Map، حجم محاسبات و تعداد ضرب‌ها کاهش می‌یابد، که این کاهش مقیاس می‌تواند به تثبیت جریان گرادیان و جلوگیری از انفجار آن کمک کند.

---

### بخش ۳: توابع فعال‌سازی و Regularization

**۹. سؤال:** تابع فعال‌سازی **ReLU (Rectified Linear Unit)** چگونه به تسریع فرآیند آموزش CNN کمک می‌کند؟
**پاسخ:** ReLU محاسبات **بسیار سریع‌تری** نسبت به توابع Sigmoid یا Tanh دارد (فقط یک مقایسه و حداکثر صفر کردن) و مشکل **اشباع گرادیان (Gradient Saturation)** را در ناحیه مثبت حل می‌کند، که منجر به همگرایی سریع‌تر می‌شود.

**۱۰. سؤال:** در صورت استفاده از ReLU، پدیده‌ی **Dying ReLU** چیست و راه حل آن کدام تابع فعال‌سازی است؟
**پاسخ:** Dying ReLU وضعیتی است که در آن نورون‌ها در طول آموزش به دلیل دریافت ورودی‌های منفی بزرگ، **به صورت دائمی غیرفعال (خاموش)** می‌شوند و دیگر گرادیانی به آن‌ها باز نمی‌گردد. راه حل، استفاده از **Leaky ReLU** یا **PReLU** است که اجازه عبور یک گرادیان کوچک (مثلاً $0.01$) را در ناحیه منفی می‌دهد.

**۱۱. سؤال:** دو تکنیک اصلی **Regularization (نظم‌دهی)** که در آموزش CNN برای جلوگیری از Overfitting استفاده می‌شود، کدامند؟
**پاسخ:**
۱. **Dropout:** غیرفعال کردن تصادفی نورون‌ها در طول آموزش.
۲. **Weight Decay (یا L2 Regularization):** اضافه کردن یک جریمه به تابع زیان بر اساس بزرگی وزن‌ها، که از رشد بیش از حد وزن‌ها جلوگیری می‌کند.

**۱۲. سؤال:** چرا در لایه آخر شبکه‌های طبقه‌بندی (Classification) چندکلاسه (Multi-Class)، معمولاً از تابع فعال‌سازی **Softmax** استفاده می‌شود؟
**پاسخ:** Softmax خروجی‌های عددی (Logits) را به یک **توزیع احتمال** تبدیل می‌کند؛ به طوری که مجموع تمام خروجی‌ها برابر با **یک** (۱) باشد و بتواند احتمال تعلق ورودی به هر کلاس را مشخص کند.

---

### بخش ۴: انواع Pooling و عملیات جایگزین

**۱۳. سؤال:** در لایه‌های Pooling، اگر Stride بزرگتر از اندازه فیلتر باشد (مثلاً $F=2, S=3$) چه اتفاقی می‌افتد و نتیجه آن چیست؟
**پاسخ:** این حالت منجر به **جا انداختن پیکسل‌ها (Skipping)** می‌شود. یعنی بین هر دو پنجره پردازش‌شده، پیکسل‌هایی از ورودی وجود دارند که توسط هیچ یک از عملیات Pooling مورد بررسی قرار نمی‌گیرند.

**۱۴. سؤال:** آیا می‌توان از Stride با مقدار ۱ در Max Pooling استفاده کرد؟ این کار چه مزیت و معایبی دارد؟
**پاسخ:** بله می‌توان.
* **مزیت:** باعث **کاهش ابعاد کمتری** می‌شود و احتمال از دست رفتن اطلاعات کمتر است.
* **عیب:** **کاهش بار محاسباتی** هدف اصلی Pooling را محقق نمی‌کند و همچنان بخش زیادی از محاسبات لایه‌های قبلی را حفظ می‌کند.

**۱۵. سؤال:** مفهوم **Global Average Pooling (GAP)** را توضیح دهید و بگویید چرا پارامترهای آموزش‌پذیر آن صفر است.
**پاسخ:** GAP یک عملیات Pooling است که در آن اندازه فیلتر برابر با **تمام ابعاد مکانی** Feature Map تنظیم می‌شود. سپس میانگین کل مقادیر هر Feature Map را محاسبه کرده و یک بردار خروجی تولید می‌کند. پارامترهای آن صفر است زیرا فقط یک عملیات **میانگین‌گیری ثابت** است.

**۱۶. سؤال:** در شبکه‌هایی که نیاز به **Local Pattern Invariance** کمتری دارند و حفظ جزئیات مکانی برای آن‌ها مهم است، چه جایگزینی برای Max Pooling توصیه می‌شود؟
**پاسخ:** استفاده از **Average Pooling** یا حذف کامل لایه‌های Pooling و به‌جای آن استفاده از **Stride > 1 در لایه‌های کانولوشن** توصیه می‌شود، تا کاهش ابعاد همراه با عملیات یادگیری انجام شود.

---

### بخش ۵: نکات طراحی معماری

**۱۷. سؤال:** در طراحی یک CNN برای تشخیص اشیاء، چرا معمولاً در لایه‌های اولیه تعداد فیلترهای کم و در لایه‌های عمیق تعداد فیلترهای زیاد استفاده می‌کنیم؟
**پاسخ:** در لایه‌های اولیه، فیلترها ویژگی‌های **جهان‌شمول (Universal)** مانند لبه‌های افقی/عمودی را استخراج می‌کنند. در لایه‌های عمیق، فیلترها باید ویژگی‌های **خاص مسأله (Problem-specific)** و انتزاعی (مانند چشم سگ یا چرخ اتومبیل) را بسازند، که نیاز به تعداد فیلترهای بیشتری برای پوشش تمام احتمالات ترکیب ویژگی‌ها دارد.

**۱۸. سؤال:** اگر ابعاد ورودی یک CNN بسیار بزرگ باشد (مثلاً $1024 \times 1024$)، برای کاهش سریع ابعاد در لایه اول چه رویکردی را پیشنهاد می‌کنید؟
**پاسخ:** استفاده از یک **کرنل بزرگ‌تر** (مثلاً $7 \times 7$) به همراه **Stride بالا** (مثلاً $S=4$) در همان لایه اول، یا استفاده از یک لایه Max Pooling با **Stride و Filter Size بالا** در ابتدای شبکه.

**۱۹. سؤال:** تکنیک **Pre-training (پیش آموزش)** در CNN چیست و چرا در پروژه‌های جدید کاربرد دارد؟
**پاسخ:** Pre-training به معنی استفاده از **وزن‌های یک مدل بزرگ** (مانند ResNet) است که قبلاً بر روی یک مجموعه داده عظیم (مانند ImageNet) آموزش داده شده است. این وزن‌ها به عنوان نقطه شروع استفاده می‌شوند (Transfer Learning) تا شبکه به جای شروع از صفر، بتواند **سریع‌تر همگرا شده** و **دقت بالاتری** را در مجموعه داده کوچک‌تر کسب کند.

**۲۰. سؤال:** تفاوت بین **Trainable Parameters** و **Non-Trainable Parameters** در یک مدل CNN را توضیح دهید.
**پاسخ:**
* **Trainable Parameters (آموزش‌پذیر):** شامل **وزن‌ها ($W$) و بایاس‌ها ($b$)** در لایه‌های Conv و FC و پارامترهای $\gamma, \beta$ در لایه‌های BN هستند که در طول بک‌پراپاگیشن به‌روز می‌شوند.
* **Non-Trainable Parameters (غیرآموزش‌پذیر):** شامل پارامترهایی مانند **Moving Mean و Moving Variance** در لایه‌های BN هستند که در طول آموزش محاسبه و به‌روز می‌شوند، اما مستقیماً توسط گرادیان آموخته نمی‌شوند. همچنین، لایه‌های Pooling و Dropout اصلاً پارامتر ندارند.

---

آیا مایلید این سؤالات را در یک سناریوی مصاحبه‌ای فرضی مرور کنیم یا نیاز به تمرکز بر موضوع دیگری دارید؟
