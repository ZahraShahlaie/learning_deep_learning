در اینجا سوالات مصاحبه‌ای (به همراه پاسخ‌های کلیدی) بر اساس مطالبی که تاکنون در مورد شبکه‌های عصبی و کارکرد عمیق‌تر آن آموخته‌اید، آورده شده است.

## ❓ سوالات مصاحبه‌ای: مبانی و کارکرد نورون

### ۱. ایده اصلی و منشأ شبکه‌های عصبی چیست؟

**پاسخ کلیدی:**
ایده اصلی از **مغز انسان** و **نورون‌های زیستی** الهام گرفته شده است. هدف این است که به جای حل مسئله با یک مدل ریاضیاتی جامع (مانند روش‌های کلاسیک)، مسئله را با ترکیب مجموعه‌ای از **ریزپردازش‌های ساده** که توسط نورون‌ها انجام می‌شود، حل کنیم.

### ۲. یک نورون مصنوعی (در غیاب تابع فعال‌سازی) از نظر ریاضیاتی چه چیزی را شبیه‌سازی می‌کند؟

**پاسخ کلیدی:**
هر نورون مصنوعی به تنهایی یک مدل **رگرسیون خطی ساده** را شبیه‌سازی می‌کند. معادله هسته‌ای آن به صورت **$Y = \sum_{i} W_i X_i + B$** است.
* **$W$ (وزن):** نقش ضریب متغیرها را دارد.
* **$B$ (بایاس):** نقش مقدار ثابت (عرض از مبدأ) را دارد.

### ۳. هدف اصلی فرآیند آموزش (Training) در یک شبکه عصبی چیست؟

**پاسخ کلیدی:**
هدف اصلی، **آپدیت (بهینه‌سازی)** مقادیر **وزن‌ها ($W$) و بایاس‌ها ($B$)** در کل شبکه است. این آپدیت‌ها با هدف **کاهش خطای (Loss)** مدل بر روی داده‌های آموزشی و رسیدن به بهترین عملکرد (حالت بهینه) انجام می‌شوند.

### ۴. نقش "وزن" و "بایاس" در معادله نورون چیست؟

**پاسخ کلیدی:**
* **وزن ($W$):** نشان‌دهنده **اهمیت** هر ورودی (ویژگی) است. وزن‌های بزرگ‌تر نشان‌دهنده اهمیت بیشتر آن ورودی در تصمیم‌گیری نورون است.
* **بایاس ($B$):** به نورون اجازه می‌دهد که **بدون توجه به ورودی‌ها**، آستانه فعال‌سازی را تغییر دهد و مدل را بهتر بر روی داده‌ها منطبق سازد (مانند تنظیم عرض از مبدأ در یک خط).

---

## 🏗️ سوالات مصاحبه‌ای: ساختار شبکه و جبر خطی

### ۵. ساختار یک شبکه عصبی فیدفوروارد (Feedforward) شامل چه لایه‌هایی است؟

**پاسخ کلیدی:**
شبکه از سه نوع لایه تشکیل می‌شود: 

[Image of a simple artificial neural network diagram with input, hidden, and output layers]

1.  **لایه ورودی (Input Layer):** داده‌های خام را دریافت می‌کند.
2.  **لایه‌های پنهان (Hidden Layers):** محاسبات و تبدیل‌های اصلی در آن‌ها انجام می‌شود.
3.  **لایه خروجی (Output Layer):** نتیجه نهایی (پیش‌بینی) مدل را تولید می‌کند.

### ۶. تعداد نورون‌ها در لایه‌های ورودی و خروجی بر چه اساسی تعیین می‌شود؟

**پاسخ کلیدی:**
* **لایه ورودی:** تعداد نورون‌ها برابر با **تعداد ویژگی‌های ورودی (Features)** در مجموعه داده است.
* **لایه خروجی:** تعداد نورون‌ها برابر با **تعداد خروجی‌های مورد نیاز** است (مثلاً در دسته‌بندی، برابر با تعداد کلاس‌ها).

### ۷. مفهوم یک شبکه "کاملاً متصل" (Fully Connected / Dense) چیست؟

**پاسخ کلیدی:**
شبکه کاملاً متصل شبکه‌ای است که در آن **خروجی هر نورون** در یک لایه به **تمام نورون‌های** لایه بعدی متصل می‌شود. این یعنی بین هر جفت نورون متوالی در لایه‌های مختلف، یک وزن مجزا وجود دارد.

### ۸. چرا برای پیاده‌سازی محاسبات لایه‌ها، از جبر خطی و ضرب ماتریسی استفاده می‌کنیم؟

**پاسخ کلیدی:**
استفاده از ضرب ماتریسی (**$Y = X \cdot W + B$**) دو مزیت اصلی دارد:
1.  **ساده‌سازی:** رابطه خطی پیچیده را به یک شکل ساده و فشرده نشان می‌دهد.
2.  **کارایی محاسباتی:** پیاده‌سازی ماتریسی برای محاسبات موازی روی سخت‌افزارهایی مانند GPUها بسیار بهینه‌تر و سریع‌تر است.

---

## 💡 سوالات مصاحبه‌ای: یادگیری و مفهوم "مسیر عصبی"

### ۹. منظور از "هایپرپارامتر" در زمینه لایه‌های پنهان چیست؟

**پاسخ کلیدی:**
**هایپرپارامترها** مقادیری هستند که **قبل از شروع** فرآیند آموزش توسط طراح مدل تنظیم می‌شوند، برخلاف وزن‌ها و بایاس‌ها که توسط فرآیند آموزش یاد گرفته می‌شوند. در لایه‌های پنهان، **تعداد لایه‌ها** و **تعداد نورون‌ها در هر لایه** جزو هایپرپارامترهای کلیدی هستند.

### ۱۰. مسیر عصبی در علم عصب‌شناسی چگونه با یادگیری شبکه عصبی ارتباط پیدا می‌کند؟

**پاسخ کلیدی:**
همانطور که شبکه‌های عصبی با تنظیم وزن‌ها یاد می‌گیرند، مغز انسان نیز با تقویت و بهینه‌سازی مسیرهای ارتباطی (سیناپس‌ها) بین نورون‌ها یاد می‌گیرد. "مسیر عصبی" به فرآیند **حذف مسیرهای بیهوده** و تقویت مسیرهای مستقیم و کارآمد در مغز اشاره دارد که منجر به افزایش **سرعت، دقت و راندمان** در یک مهارت خاص می‌شود.
