
---

## 📝 سؤالات و پاسخ‌های مصاحبه (شبکه‌های عصبی و پرسپترون‌ها)

### ۱. مفاهیم پایه شبکه عصبی
#### سؤال ۱: تفاوت بین **نورون** و **پرسپترون** چیست؟
* **پاسخ:**
    * **نورون (Neuron):** به **واحد پردازشی** بنیادی در شبکه عصبی گفته می‌شود که ورودی می‌گیرد، محاسبه انجام می‌دهد و خروجی می‌دهد.
    * **پرسپترون (Perceptron):** یک اصطلاح کلی برای این واحد محاسباتی است که شامل ورودی‌ها، محاسبه **جمع وزنی** و تولید خروجی است.

#### سؤال ۲: لایه‌های اصلی یک شبکه عصبی کدامند؟
* **پاسخ:** سه لایه اصلی وجود دارد:
    1.  **لایه ورودی (Input Layer):** تعداد گره‌ها برابر با تعداد ویژگی‌های ورودی است.
    2.  **لایه‌های مخفی (Hidden Layers):** لایه‌های میانی که محاسبات و استخراج ویژگی‌های پیچیده در آن‌ها انجام می‌شود.
    3.  **لایه خروجی (Output Layer):** خروجی نهایی مدل را تولید می‌کند.

### ۲. وزن و بایاس (Weights and Bias)
#### سؤال ۳: نقش **وزن‌ها ($W$)** در یک نورون چیست؟
* **پاسخ:** وزن‌ها تعیین می‌کنند که هر ورودی چقدر در محاسبه خروجی اهمیت دارد. در فرآیند آموزش، وزن‌ها بهینه‌سازی می‌شوند تا مدل بتواند الگوهای صحیح را بیاموزد.

#### سؤال ۴: **بایاس ($B$)** چیست و چه وظایف مهمی را در پرسپترون انجام می‌دهد؟
* **پاسخ:** بایاس یک مقدار ثابت است که به جمع وزنی اضافه می‌شود.
    * **وظیفه اول:** عملکردی مشابه **عرض از مبدأ** در رگرسیون خطی دارد.
    * **وظیفه دوم (بسیار مهم):** جلوگیری از **سوگیری (Bias) مدل** و مهم‌تر از آن، جلوگیری از **نادیده گرفته شدن مقادیر صفر معنی‌دار** در ورودی‌ها. اگر ورودی صفر باشد، حاصل‌ضرب آن در وزن صفر می‌شود؛ بایاس تضمین می‌کند که نورون همچنان بتواند فعال شود.

### ۳. تابع فعال‌سازی (Activation Function)
#### سؤال ۵: چرا به تابع فعال‌سازی نیاز داریم، در حالی که جمع وزنی محاسبات را انجام می‌دهد؟
* **پاسخ:**
    * محاسبه **جمع وزنی** یک عملیات **خطی (Linear)** است.
    * **تابع فعال‌سازی** (که اغلب **غیرخطی** است) به شبکه این امکان را می‌دهد که **روابط غیرخطی و پیچیده** در داده‌ها را یاد بگیرد. بدون آن، صرف نظر از تعداد لایه‌ها، کل شبکه در حد یک مدل خطی ساده باقی می‌ماند.

#### سؤال ۶: کارکرد تابع سیگموئید (Sigmoid) چیست؟ و چه مزیتی دارد؟
* **پاسخ:**
    * **فرمول:** $f(z) = \frac{1}{1 + e^{-z}}$
    * **مزیت:** خروجی این تابع همیشه در محدوده **$[0, 1]$** قرار دارد و باعث **نرمال‌سازی (Normalization)** مقادیر خروجی نورون می‌شود، که این کار به پایداری فرآیند آموزش کمک می‌کند.

### ۴. آموزش و بهینه‌سازی مدل
#### سؤال ۷: وزن‌ها در ابتدای فرآیند آموزش چگونه تنظیم می‌شوند؟
* **پاسخ:** در شروع فرآیند آموزش، تمام وزن‌ها و بایاس‌ها به صورت **تصادفی (Random)** مقداردهی می‌شوند.

#### سؤال ۸: منظور از اینکه شبکه عصبی یک **"جعبه سیاه" (Black Box)** است، چیست؟ و هدف ما به عنوان مهندس/دانشمند داده چیست؟
* **پاسخ:**
    * **جعبه سیاه:** به این معنی است که به دلیل پیچیدگی و تعداد زیاد لایه‌ها و وزن‌ها، **توجیه دقیق** اینکه مدل در لایه‌های میانی چه الگوهایی را آموخته و چرا رفتار خاصی از خود نشان می‌دهد، دشوار است.
    * **هدف:** هدف اصلی ما این است که **رفتار شبکه** و **الگوریتم‌های آموزش** (مانند بک‌پروپگیشن) را بشناسیم تا بتوانیم **هایپرپارامترها** (مانند نوع تابع فعال‌سازی) را به درستی تنظیم کرده و مشکلات آموزش (مانند انفجار یا محوشدگی گرادیان) را حل کنیم.

---

