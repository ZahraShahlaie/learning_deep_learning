در اینجا یک جزوه بسیار خلاصه و کاربردی با نکات کلیدی از مطالب ارائه شده در مورد الگوریتم پس‌انتشار خطا (Backpropagation) آمده است.

---

# 🔑 نکات کلیدی: الگوریتم پس‌انتشار خطا (Backpropagation)

## ۱. هدف و مراحل کلی

هدف اصلی الگوریتم بک‌پراپگیشن، **بهینه‌سازی (Optimal) وزن‌ها ($W$) و بایاس‌ها ($B$)** در شبکه عصبی برای کاهش خطا است.

| مرحله | نام فرآیند | جهت حرکت | هدف |
| :---: | :---: | :---: | :---: |
| ۱ | **پیشروی (Feedforward)** | ورودی $\rightarrow$ خروجی | محاسبه خروجی و **خطای نهایی (Loss)** |
| ۲ | **پس‌انتشار (Backpropagation)** | خروجی $\rightarrow$ ورودی | محاسبه **گرادیان** و آپدیت پارامترها |

---

## ۲. محاسبه و انتشار خطا 📉

 **تابع هزینه (Loss/Cost Function):** ابزاری برای سنجش اختلاف بین خروجی پیش‌بینی شده ($\hat{Y}$) و خروجی واقعی ($Y$).

 **توزیع خطا:** خطای محاسبه شده در لایه خروجی، به صورت **متناسب با وزن‌های** اتصالات، به لایه‌های قبلی برگردانده و توزیع می‌شود.

  $$
     \text{Error}_{\text{Connection}} \propto W_{\text{Connection}}
     $$
    
**اصل بقای خطا:** مجموع خطاهای توزیع شده در هر لایه (مجموع خطاهای نودها) باید همواره برابر با خطای نهایی باشد.

---

## ۳. به‌روزرسانی پارامترها (Optimization)

به‌روزرسانی وزن‌ها و بایاس‌ها بر اساس الگوریتم **گرادیان کاهشی (Gradient Descent)** انجام می‌شود.

### الف) نقش گرادیان

* **گرادیان ($\frac{\partial L}{\partial W}$):** مشتق جزئی تابع هزینه ($L$) نسبت به پارامتر (وزن یا بایاس). این مقدار، جهت و میزان تغییر لازم برای **کاهش** هزینه را مشخص می‌کند.
* **نرخ یادگیری ($\alpha$):** میزان گام (Step Size) در جهت گرادیان.

### ب) فرمول اصلی به‌روزرسانی

$$
\text{Parameter}_{\text{new}} = \text{Parameter}_{\text{old}} - \alpha \times \text{Gradient}
$$

### ج) دلتا و مشتق تابع فعال‌سازی

فرمول گرادیان در شبکه‌های عصبی از **قانون زنجیره‌ای (Chain Rule)** مشتقات پیروی می‌کند و شامل یک فاکتور کلیدی به نام **دلتا ($\delta$)** است.

$$
\text{Gradient} = \underbrace{\frac{\partial L}{\partial Y} \times \frac{\partial Y}{\partial Z}}_{\text{Delta } (\delta)} \times \frac{\partial Z}{\partial W}
$$

1.  **ضرورت مشتق‌پذیری:** **تابع فعال‌سازی ($f$)** حتماً باید **مشتق‌پذیر** باشد، زیرا در محاسبه $\delta$ از مشتق آن ($f'(Z)$) استفاده می‌شود.
2.  **آپدیت وزن ($W$):**
    $$
    W_{\text{new}} = W_{\text{old}} - \alpha \times \delta \times X \quad (\frac{\partial Z}{\partial W} = X)
    $$
3.  **آپدیت بایاس ($B$):**
    $$
    B_{\text{new}} = B_{\text{old}} - \alpha \times \delta \quad (\frac{\partial Z}{\partial B} = 1)
    $$

---

## ۴. چرخه آموزشی (Iteration) 🔄

فرآیند کامل آموزش شبکه به صورت تکراری است:

**۱ تکرار (Iteration) = ۱ گام پیشروی + ۱ گام پس‌انتشار + ۱ آپدیت وزن و بایاس**

این فرآیند تا زمانی که شبکه به **حالت بهینه (Minimum Loss)** برسد، تکرار می‌شود. 

