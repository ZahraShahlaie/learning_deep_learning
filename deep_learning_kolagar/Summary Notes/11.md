بسیار خوب، در اینجا یک **جزوه خلاصه شده و متمرکز بر نکات کلیدی** از مباحث محاسبه پارامترهای شبکه‌های عصبی (Fully Connected و Convolutional) ارائه شده است.

---

## 📝 خلاصه نکات کلیدی: محاسبه پارامترهای شبکه‌های عصبی

### ۱. اهمیت پارامترهای یادگیری

| نکته کلیدی | توضیح و اهمیت |
| :--- | :--- |
| **تعریف پارامتر** | **وزن‌ها ($W$) و بایاس‌ها ($b$)** که تنها مقادیری هستند که در طول آموزش **آپدیت** شده و توسط شبکه **یاد گرفته می‌شوند.** |
| **بار پردازشی** | تعداد پارامترها مستقیماً با **نیاز سخت‌افزاری** (GPU/حافظه) و **زمان آموزش** (هزینه یادگیری) ارتباط دارد. |
| **پیچیدگی مدل** | تعداد پارامترها معیار اصلی برای سنجش **پیچیدگی شبکه** و ظرفیت آن برای مدل‌سازی روابط پیچیده در داده است. |
| **ساختار نورون** | هر نورون یک **جمع وزنی** انجام می‌دهد: $\text{Output} = f(\sum (X_i \cdot W_i) + b)$. پارامترهای یادگیری همان $W_i$ و $b$ هستند. |

---

### ۲. محاسبه پارامترها در لایه‌های Fully Connected (FC)

این لایه‌ها برای هر نورون ورودی‌ها را از **تمام** خروجی‌های لایه قبلی دریافت می‌کنند.

| عنصر پارامتری | نحوه محاسبه |
| :--- | :--- |
| **وزن‌های هر نورون** | تعداد ورودی‌ها (برابر با تعداد یونیت‌های لایه قبلی) |
| **بایاس‌های هر نورون** | ۱ (هر نورون فقط یک بایاس دارد) |
| **کل پارامترهای لایه** | $(\text{تعداد ورودی} + 1) \times \text{تعداد یونیت‌های لایه}$ |

**نکات مهم FC:**

* **وابستگی به ابعاد ورودی:** در لایه FC اول، تعداد پارامترها **مستقیماً** به **تعداد ویژگی‌های ورودی (Input Shape)** وابسته است.
* **Flatten:** تصاویر قبل از ورود به لایه FC باید **یک‌بُعدی (Flatten)** شوند، که این کار منجر به **حذف روابط فضایی** و افزایش شدید پارامترها می‌شود.

---

### ۳. محاسبه پارامترها در لایه‌های Convolutional (Conv)

این لایه‌ها از فیلترها (کرنل‌ها) برای استخراج ویژگی‌های محلی استفاده می‌کنند و از اشتراک‌گذاری وزن بهره می‌برند.

| عنصر پارامتری | نحوه محاسبه |
| :--- | :--- |
| **وزن‌های یک فیلتر** | $\text{اندازه کرنل (ارتفاع } \times \text{ عرض)} \times \text{عمق ورودی (تعداد کانال‌ها)}$ |
| **بایاس یک فیلتر** | ۱ (برای هر فیلتر فقط یک بایاس وجود دارد) |
| **کل پارامترهای لایه** | $(\text{تعداد وزن‌های یک فیلتر} + 1) \times \text{تعداد فیلترها}$ |

**نکات مهم Conv:**

| نکته کلیدی | ویژگی |
| :--- | :--- |
| **اشتراک‌گذاری وزن** | وزن‌های یک فیلتر در **تمام نقاط تصویر** به کار می‌روند. این دلیل اصلی **کاهش چشمگیر پارامترها** است.  |
| **بی‌تاثیری ابعاد مکانی** | ابعاد $N \times N$ تصویر ورودی (اندازه پیکسل‌ها) **هیچ تأثیری** بر تعداد پارامترهای یادگیری **ندارد**. |
| **اهمیت عمق** | عمق فیلترها (تعداد کانال‌ها) باید **برابر با عمق ورودی** باشد و مستقیماً روی تعداد وزن‌ها تأثیر می‌گذارد. |
| **استخراج ویژگی** | فیلترها الگوهای محلی (لبه‌ها، بافت‌ها) را استخراج می‌کنند و باعث می‌شوند مدل نسبت به جابه‌جایی شیء (Translation Invariance) قوی باشد. |

---

### ۴. پارامترهای لایه‌های جانبی (Side Layers)

| لایه | پارامترهای قابل آموزش | نحوه محاسبه پارامترها |
| :--- | :--- | :--- |
| **Max/Avg Pooling** | **صفر** | فقط عملیات نمونه‌برداری و کاهش ابعاد مکانی را انجام می‌دهد. |
| **Dropout** | **صفر** | فقط برخی نورون‌ها را به صورت تصادفی غیرفعال می‌کند. |
| **Batch Normalization (BN)** | **دارد** | $\mathbf{2 \times \text{تعداد کانال‌های ورودی}}$ (برای $\gamma$ و $\beta$). |

