بسیار خوب، در اینجا یک جزوه خلاصه شده با تمرکز بر نکات کلیدی و کاربردی توابع فعال‌سازی (Activation Functions) در شبکه‌های عصبی برای شما تهیه شده است:

---

## 💡 خلاصه نکات کلیدی توابع فعال‌سازی

### ۱. هدف اصلی توابع فعال‌سازی

تابع‌های فعال‌سازی عملیاتی هستند که بر روی **جمع وزنی** (Weighted Sum - $Z$) ورودی‌های یک نورون انجام می‌شوند و خروجی نهایی نورون را تعیین می‌کنند.

* **هدف حیاتی:** ایجاد **غیرخطی بودن (Non-linearity)** در شبکه.
    * بدون این توابع، هر شبکه عصبی عمیق (با چندین لایه) به یک مدل خطی ساده تقلیل می‌یابد.
* **محل استفاده:** هم در **لایه‌های پنهان (Hidden Layers)** و هم در **لایه خروجی (Output Layer)** استفاده می‌شوند.

---

### ۲. چالش‌های اصلی در انتخاب تابع فعال‌سازی

| چالش | توضیح | تابع‌های دارای این مشکل |
| :--- | :--- | :--- |
| **محوشدگی گرادیان (Vanishing Gradient)** | مشتق تابع فعال‌سازی در نواحی انتهایی به سمت صفر میل می‌کند. این باعث می‌شود که در فرآیند **پس‌انتشار خطا (Backpropagation)**، به‌روزرسانی وزن‌های لایه‌های اولیه شبکه متوقف شود یا بسیار کند صورت گیرد. | **Sigmoid**، **Tanh** |
| **مرگ نورون‌ها (Dying ReLU)** | زمانی که ورودی نورون منفی باشد، خروجی و مشتق آن صفر می‌شود. این نورون‌ها به‌طور دائمی غیرفعال می‌شوند و دیگر در فرآیند آموزش مشارکت نمی‌کنند. | **ReLU** |

---

### ۳. بررسی توابع فعال‌سازی مهم و کاربرد آن‌ها

#### الف. توابع قدیمی‌تر (مشکل‌ساز در شبکه‌های عمیق)

| تابع | بازه خروجی | مزیت/نقطه قوت | مشکل اصلی |
| :--- | :--- | :--- | :--- |
| **سیگموید (Sigmoid)** | $[0, 1]$ | خروجی را به یک احتمال تبدیل می‌کند. | **محوشدگی گرادیان** در ورودی‌های بزرگ و کوچک. |
| **تانژانت هذلولوی (Tanh)** | $[-1, 1]$ | متقارن حول صفر است (مرکزگرا). | **محوشدگی گرادیان** شدیدتر از سیگموید. |
| **خطی (Linear)** | $(-\infty, \infty)$ | ساده‌ترین تابع. | شبکه را خطی می‌کند و قابلیت یادگیری الگوهای پیچیده را از بین می‌برد. |

---

#### ب. توابع مدرن و پرکاربرد (راه‌حل‌های بهینه‌سازی)

| تابع | بازه خروجی | رابطه | مزیت/راه‌حل مشکل |
| :--- | :--- | :--- | :--- |
| **رِلو (ReLU)** | $[0, \infty)$ | $\max(0, Z)$ | **سرعت بالا** در محاسبه و حل مشکل محوشدگی گرادیان برای مقادیر مثبت (مشتق همیشه ۱ است).  |
| **رِلو نشت‌کننده (Leaky ReLU)** | $(-\infty, \infty)$ | $\alpha Z$ برای $Z \le 0$ | با ارائه یک شیب کوچک ($\alpha \approx 0.01$) برای مقادیر منفی، مشکل **مرگ نورون‌ها** را حل می‌کند. |
| **ELU** | $(-\alpha, \infty)$ | $\alpha(e^Z - 1)$ برای $Z \le 0$ | مانند Leaky ReLU مشکل ورودی‌های منفی را حل می‌کند. |

---

#### ج. توابع مخصوص لایه خروجی

| تابع | کاربرد در لایه خروجی | ویژگی |
| :--- | :--- | :--- |
| **سافت‌مکس (Softmax)** | **دسته‌بندی چندگانه** (Multi-class Classification) | خروجی‌ها را به یک **توزیع احتمال** تبدیل می‌کند؛ مجموع تمام خروجی‌ها برابر با ۱ خواهد بود.  |
| **سیگموید (Sigmoid)** | **دسته‌بندی دودویی** (Binary Classification) | خروجی را بین ۰ و ۱ محدود می‌کند (احتمال وقوع کلاس ۱). |
| **خطی (Linear)** | **رگرسیون** (Regression) | هیچ تبدیلی روی خروجی انجام نمی‌دهد. |

---

### ۴. نتیجه‌گیری عملی

1.  **لایه‌های پنهان:**
    * امروزه، **ReLU** و زیرشاخه‌های آن (**Leaky ReLU** و **ELU**) انتخاب‌های پیش‌فرض و برتر هستند.
    * **ReLU** معمولاً شروع خوبی است، اما اگر مشکل **Dying ReLU** مشاهده شد، به **Leaky ReLU** یا **ELU** تغییر دهید.

2.  **لایه خروجی:**
    * انتخاب تابع فعال‌سازی مستقیماً به نوع مسئله بستگی دارد (طبق جدول بالا).

این دانش به شما کمک می‌کند تا هنگام **عیب‌یابی** مدل، درک کنید که اگر دقت (Accuracy) شبکه بالا نمی‌رود یا فرآیند آموزش متوقف می‌شود، ممکن است مشکل از انتخاب نادرست یا عملکرد نامطلوب تابع فعال‌سازی (مانند **Vanishing Gradient**) باشد.

---

