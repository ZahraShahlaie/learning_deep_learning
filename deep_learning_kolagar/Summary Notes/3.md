

حتماً، در اینجا یک جزوه خلاصه شده با تأکید بر نکات کلیدی مباحث مطرح شده در مورد شبکه‌های عصبی و پرسپترون‌ها آورده شده است.

---

## 💡 نکات کلیدی: شبکه‌های عصبی و پرسپترون‌ها

### ۱. مفاهیم پایه
| اصطلاح | تعریف |
| :--- | :--- |
| **نورون (Neuron)** | واحد پردازشی اصلی در شبکه عصبی است. |
| **پرسپترون (Perceptron)** | نام کلی برای واحدی است که شامل ورودی‌ها، محاسبات، و خروجی است. |

---

### ۲. محاسبه خروجی نورون
خروجی هر نورون از دو مرحله کلیدی تشکیل شده است:

* **۱. جمع وزنی (Weighted Sum):**
    * این مرحله یک عملیات **خطی (Linear)** است.
    * محاسبه: حاصل‌ضرب بردار ورودی $(\mathbf{X})$ در ماتریس وزن $(\mathbf{W})$ به اضافه بردار بایاس $(\mathbf{B})$.
    $$\mathbf{Z} = \mathbf{X} \cdot \mathbf{W} + \mathbf{B}$$
* **۲. تابع فعال‌سازی (Activation Function):**
    * خروجی جمع وزنی $(\mathbf{Z})$ به این تابع اعمال می‌شود.
    * $$\text{Output} = f(\mathbf{Z})$$
    * **نکته حیاتی:** استفاده از توابع فعال‌سازی **غیرخطی (Non-Linear)**، شبکه را قادر می‌سازد تا **روابط و مسائل پیچیده‌تر** را حل کند.

---

### ۳. وزن و بایاس (Weights and Bias)

| عنصر | نقش کلیدی | اهمیت |
| :--- | :--- | :--- |
| **وزن‌ها $(\mathbf{W})$** | تعیین‌کننده میزان اهمیت هر ورودی در محاسبات. | در شروع، **تصادفی** مقداردهی شده و در حین آموزش **بهینه‌سازی** می‌شوند. |
| **بایاس $(\mathbf{B})$** | ۱. **عرض از مبدأ** خطی. ۲. جلوگیری از **سوگیری (Bias)** و نادیده گرفتن **صفر**های معنی‌دار در ورودی. | تضمین می‌کند که نورون حتی با ورودی‌های صفر، فعال بماند. |

---

### ۴. لایه‌های شبکه (Network Layers)

| لایه | وظیفه |
| :--- | :--- |
| **ورودی (Input Layer)** | دریافت ویژگی‌های اولیه داده. |
| **مخفی (Hidden Layer)** | انجام محاسبات اصلی و استخراج ویژگی‌های پیچیده. |
| **خروجی (Output Layer)** | ارائه نتیجه نهایی مدل. |

---

### ۵. فرآیند آموزش (Training Process)

1.  **مقداردهی اولیه:** وزن‌ها به صورت **رندوم** شروع می‌شوند.
2.  **محاسبه خطا:** استفاده از **تابع زیان (Loss Function)** یا **تابع هزینه (Cost Function)** برای اندازه‌گیری اختلاف بین خروجی مدل و خروجی واقعی.
3.  **آپدیت وزن‌ها:** استفاده از الگوریتم **انتشار به عقب (Backpropagation)** برای تنظیم وزن‌ها به منظور **کاهش خطا** (بهینه‌سازی).

---

### ۶. تابع فعال‌سازی (Activation Function)

* **کارکرد اصلی:** اضافه کردن خاصیت **غیرخطی** به محاسبات خطی جمع وزنی.
* **مزایای دیگر:** برخی توابع مانند **سیگموئید**، خروجی‌ها را در بازه $[0, 1]$ **نرمال‌سازی** می‌کنند.
* **انتخاب:** نوع تابع فعال‌سازی یک **هایپرپارامتر** است و بسته به نوع مسئله (رگرسیون، طبقه‌بندی و...) انتخاب می‌شود.

---

### ۷. جعبه سیاه (Black Box)

* **مفهوم:** به دلیل پیچیدگی محاسبات در لایه‌های میانی، توجیه دقیق اینکه مدل **چه چیزی** را آموخته است، همیشه ممکن نیست.
* **هدف عملی:** تمرکز بر درک **الگوریتم‌های آموزش و بهینه‌سازی** و چگونگی تنظیم **هایپرپارامترها** (مانند نرخ یادگیری، نوع تابع فعال‌سازی و...) برای دستیابی به بهترین عملکرد مدل است.

