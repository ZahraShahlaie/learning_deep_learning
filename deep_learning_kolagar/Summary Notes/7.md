## 📚 خلاصه نکات کلیدی: ایپاک و بچ در آموزش شبکه عصبی

این جزوه خلاصه، مفاهیم اصلی **اِیپاک** و **بچ** و تأثیر آن‌ها بر فرآیند آموزش شبکه‌های عصبی را به صورت کاربردی بیان می‌کند.

---

### ۱. اِیپاک (Epoch)

**اِیپاک** واحد اندازه‌گیری یک دور کامل آموزش شبکه است.

* **تعریف:** یک ایپاک عبارت است از **یک دور کامل** پردازش کل **داده‌های آموزشی** توسط شبکه.
* **عملکرد:** در طول یک ایپاک، شبکه کل دیتاست را می‌بیند، خطا محاسبه می‌شود، و در نهایت، وزن‌های شبکه بر اساس اطلاعات کل داده‌ها (یا مینی‌بچ‌های آن‌ها) به‌روزرسانی می‌شود.
* **هدف:** تکرار چندین ایپاک (مثلاً ۱۰ تا ۱۰۰۰) برای کاهش تدریجی خطا و رسیدن مدل به حالت بهینه.

---

### ۲. بچ (Batch) و مینی-بچ (Mini-Batch)

**بچ (Batch)** بخشی کوچک از کل دیتاست است که در هر گام به شبکه داده می‌شود.

| مفهوم | تعریف | چرا استفاده می‌شود؟ (چالش‌های GD) |
| :--- | :--- | :--- |
| **بچ سایز (Batch Size)** | تعداد نمونه‌هایی که در یک مرحله به شبکه داده شده و وزن‌ها بر اساس خطای آن‌ها به‌روز می‌شوند (یک **هایپرپارامتر** قابل تنظیم). | ۱. **محدودیت حافظه (Memory Limitation):** عدم توانایی GPU در ذخیره کل دیتاست‌های بزرگ. ۲. **سرعت (Speed):** افزایش سرعت آموزش با فعال‌سازی پردازش موازی. |
| **مینی-بچ (Mini-Batch)** | دسته‌های کوچکی که کل دیتاست را به بخش‌های قابل مدیریت تقسیم می‌کنند. | استفاده از مینی-بچ‌ها مشکل حافظه را حل کرده و امکان بهره‌برداری از **پردازش موازی (Parallel Processing)** در GPUها را فراهم می‌سازد، که در نهایت زمان آموزش را کاهش می‌دهد. |

---

### ۳. ارتباط مفاهیم و الگوریتم‌های بهینه‌سازی

| الگوریتم | رویکرد | تعداد آپدیت وزن در یک ایپاک |
| :--- | :--- | :--- |
| **گرادیان نزولی (GD)** | پردازش **کل دیتاست** در هر گام. | **۱ بار** (پس از دیدن تمام داده‌ها). |
| **گرادیان نزولی تصادفی (SGD)** | پردازش **یک نمونه** در هر گام. | برابر با **تعداد کل نمونه‌های داده**. |
| **گرادیان نزولی مینی-بچ (Mini-Batch GD)** | پردازش **یک مینی-بچ** (دسته‌ای از نمونه‌ها) در هر گام. | برابر با **تعداد مینی-بچ‌های** تشکیل دهنده دیتاست (مثلاً اگر ۱۰۰۰ نمونه و بچ ۲۰ تایی باشد، $1000/20 = 50$ بار). |

---

### ۴. نکات کلیدی و تحلیلی

* **سرعت:** علی‌رغم آپدیت‌های مکرر، Mini-Batch GD سریع‌تر از GD است؛ زیرا **سخت‌افزار (GPU)** می‌تواند نمونه‌های هر بچ را به صورت **موازی** پردازش کند.
* **هایپرپارامتر:** **بچ سایز** یک هایپرپارامتر مهم است. انتخاب سایز بهینه می‌تواند تعادل بین سرعت آموزش (سایز بزرگتر) و کیفیت همگرایی (سایز کوچکتر) را فراهم کند.
* **SGD و Mini-Batch:** الگوریتم‌های بهینه‌سازی پیشرفته مانند Adam، RMSprop و Adagrad در واقع نسخه‌های بهبود یافته و سریع‌تر از **Mini-Batch Gradient Descent** هستند.

---

