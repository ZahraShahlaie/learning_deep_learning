## 🧠 تابع‌های فعال‌سازی (Activation Functions) در شبکه‌های عصبی

این جزوه بر اساس محتوای ارائه شده درباره **تابع‌های فعال‌سازی** در شبکه‌های عصبی تهیه شده است. این تابع‌ها نقش حیاتی در تعیین کارایی مدل، به ویژه در ایجاد **غیرخطی بودن** (Non-linearity) و مدیریت فرآیند **بهینه‌سازی** (Optimization) دارند.

---

### ۱. مقدمه و اهمیت تابع‌های فعال‌سازی

تابع‌های فعال‌سازی عملیات نهایی را بر روی **جمع وزنی** (Weighted Sum - $Z$) ورودی‌های یک نورون انجام می‌دهند.

* **اهمیت:** تابع‌های فعال‌سازی شدیداً بر کارایی مدل و حل‌پذیری مسائل تأثیر می‌گذارند. انتخاب نادرست می‌تواند کارایی مدل را تضعیف کند یا فرآیند آموزش را متوقف سازد.
* **وظایف اصلی:**
    1.  **ایجاد غیرخطی بودن:** این مهم‌ترین مزیت است. اگر از تابع فعال‌سازی خطی استفاده کنیم، هر تعداد لایه را می‌توان به یک لایه خطی ساده تقلیل داد و شبکه قادر به یادگیری الگوهای پیچیده و غیرخطی (Complex Data) نخواهد بود.
    2.  **کنترل خروجی:** محدود کردن خروجی به یک محدوده مشخص (مانند [۰, ۱] یا [-۱, ۱]).
* **محل قرارگیری:** برای هر دو لایه‌های **پنهان** (Hidden Layers) و **خروجی** (Output Layer) باید تابع فعال‌سازی مناسب تعریف شود.

---

### ۲. بررسی تابع‌های فعال‌سازی پرکاربرد

#### ۲.۱. تابع فعال‌سازی خطی (Linear Activation Function)
* **رابطه:** $f(x) = x$ یا $f(x) = ax$.
* **مشتق:** $f'(x) = 1$ (در حالت $f(x) = x$) یا $f'(x) = a$.
* **کاربرد:** خروجی تابع خطی، جمع وزنی ورودی‌ها را تغییر نمی‌دهد. در عمل، از این تابع فعال‌سازی به ندرت در لایه‌های پنهان استفاده می‌شود، زیرا باعث خطی شدن کل شبکه می‌شود. با این حال، در مسائل **رگرسیون** (Regression) در **لایه خروجی** کاربرد دارد.


#### ۲.۲. تابع فعال‌سازی سیگموید (Sigmoid Activation Function)
* **رابطه:** $f(x) = \frac{1}{1 + e^{-x}}$
* **بازه خروجی:** $[0, 1]$ (هیچ‌گاه به صفر یا یک نمی‌رسد، اما به آن‌ها میل می‌کند).
* **کاربرد:** این تابع خروجی را به محدوده‌ای بین ۰ و ۱ می‌برد که یادآور مفهوم **احتمال** (Probability) است. به همین دلیل، اغلب در **لایه خروجی** برای مسائل **دسته‌بندی دودویی** (Binary Classification) استفاده می‌شود. اگر خروجی بالای ۰.۵ باشد، نشان‌دهنده کلاس ۱ و اگر کمتر باشد، کلاس ۰ در نظر گرفته می‌شود.
* **مشکل:**
    * **محوشدگی گرادیان (Vanishing Gradient):** زمانی که مقادیر ورودی ($x$) بسیار بزرگ یا بسیار کوچک (مثبت یا منفی بی‌نهایت) می‌شوند، شیب تابع (مشتق) به سمت **صفر** میل می‌کند.


#### ۲.۳. تابع فعال‌سازی تانژانت هذلولوی (Hyperbolic Tangent - Tanh)
* **رابطه:** $\tanh(x) = \frac{e^x - e^{-x}}{e^x + e^{-x}}$
* **بازه خروجی:** $[-1, 1]$ (نسبت به سیگموید که متقارن نیست، متقارن حول صفر است).
* **مشکل:**
    * مشابه سیگموید، در مقادیر ورودی بسیار بزرگ یا بسیار کوچک، مشتق به سمت **صفر** میل می‌کند و مشکل **محوشدگی گرادیان** رخ می‌دهد.


---

### ۳. مشکل محوشدگی گرادیان (Vanishing Gradient)

این مشکل در سیگموید و تانژانت هذلولوی باعث کندی شدید یا توقف فرآیند آموزش می‌شود:

* **فرآیند آموزش (Backpropagation):** در مرحله پس‌انتشار خطا، برای به‌روزرسانی وزن‌ها از فرمول مشتق استفاده می‌شود: $\frac{\partial L}{\partial W} = \frac{\partial L}{\partial y} \cdot f'(Z)$.
* **نقش مشتق تابع فعال‌سازی ($f'(Z)$):** اگر مشتق تابع فعال‌سازی ($f'(Z)$) در یک لایه بسیار کوچک باشد (نزدیک به صفر)، این مقدار در طول فرآیند پس‌انتشار در مشتقات لایه‌های قبلی ضرب شده و باعث می‌شود که گرادیان (میزان تغییر وزن‌ها) لایه‌های اولیه نیز بسیار کوچک شود.
* **نتیجه:** در نهایت، به‌روزرسانی وزن‌ها در لایه‌های عمیق‌تر شبکه متوقف می‌شود یا بسیار کند انجام می‌گیرد، که به آن **محوشدگی گرادیان** یا **Vanishing Gradient** می‌گویند.

---

### ۴. تابع‌های فعال‌سازی مبتنی بر ReLU و زیرشاخه‌های آن

برای حل مشکل محوشدگی گرادیان، تابع‌های فعال‌سازی جدیدی معرفی شدند که مشکل شیب صفر در یک جهت را برطرف کردند.

#### ۴.۱. تابع فعال‌سازی رِلو (Rectified Linear Unit - ReLU)
* **رابطه:** $f(Z) = \max(0, Z)$
    * اگر $Z > 0$ باشد، خروجی $Z$ است.
    * اگر $Z \le 0$ باشد، خروجی $0$ است.
* **مشتق:**
    * اگر $Z > 0$ باشد، مشتق $1$ است.
    * اگر $Z < 0$ باشد، مشتق $0$ است.
* **مزایا:**
    * مشکل **محوشدگی گرادیان** را برای مقادیر مثبت حل می‌کند (مشتق همیشه ۱ است).
    * محاسبه ساده‌ای دارد (سریع‌تر از سیگموید و تانژانت هذلولوی).
* **مشکل:**
    * **مرگ نورون‌های رِلو (Dying ReLU):** برای تمام ورودی‌های منفی، خروجی و مشتق تابع **صفر** است. این باعث می‌شود که نورون‌هایی که خروجی منفی داشته‌اند، کاملاً غیرفعال و خاموش شوند. این نورون‌ها دیگر هیچ تأثیری در لایه‌های بعدی نخواهند داشت و در طول آموزش نیز وزن‌هایشان به‌روز نمی‌شود (چون گرادیان صفر است). این امر می‌تواند بخش بزرگی از شبکه را از دسترس خارج کند.


#### ۴.۲. تابع فعال‌سازی رِلو نشت‌کننده (Leaky ReLU)
* **رابطه:**
    * اگر $Z > 0$ باشد، $f(Z) = Z$
    * اگر $Z \le 0$ باشد، $f(Z) = \alpha Z$ (که $\alpha$ یک ضریب کوچک، مثلاً $0.01$ است).
* **مشتق:**
    * اگر $Z > 0$ باشد، مشتق $1$ است.
    * اگر $Z \le 0$ باشد، مشتق $\alpha$ است.
* **مزیت:** با داشتن یک شیب کوچک ($\alpha$) برای ورودی‌های منفی، مشکل **مرگ نورون‌های رِلو** را حل می‌کند. نورون‌ها همچنان برای ورودی‌های منفی یک گرادیان کوچک (غیر صفر) دارند و می‌توانند به‌روزرسانی شوند.

#### ۴.۳. تابع فعال‌سازی واحد خطی نمایی (Exponential Linear Unit - ELU)
* **رابطه:**
    * اگر $Z > 0$ باشد، $f(Z) = Z$
    * اگر $Z \le 0$ باشد، $f(Z) = \alpha(e^Z - 1)$
* **مزیت:** مانند Leaky ReLU مشکل ورودی‌های منفی را حل کرده و سرعت همگرایی را بهبود می‌بخشد.

---

### ۵. تابع فعال‌سازی سافت‌مکس (Softmax Activation Function)

* **کاربرد:** منحصراً برای **لایه خروجی** در مسائل **دسته‌بندی چندگانه** (Multi-class Classification) استفاده می‌شود.
* **هدف:** خروجی‌های جمع وزنی (Logits) را به یک توزیع احتمال تبدیل می‌کند؛ به طوری که مجموع تمام خروجی‌ها برابر با **یک** شود.
* **رابطه:** برای هر خروجی $i$:
    $$P_i = \frac{e^{Z_i}}{\sum_{j=1}^{K} e^{Z_j}}$$
    * $Z_i$: خروجی جمع وزنی برای کلاس $i$.
    * $K$: تعداد کل کلاس‌ها.
* **نحوه کار:** سافت‌مکس از تابع نمایی ($e^Z$) استفاده می‌کند تا تفاوت‌ها بین خروجی‌ها را بزرگنمایی کند. خروجی با بیشترین مقدار (Logit) بیشترین احتمال را به خود اختصاص می‌دهد. مدل، کلاسی را انتخاب می‌کند که بالاترین احتمال را داشته باشد.

---

### ۶. جمع‌بندی نکات کلیدی

| مرحله آموزش/لایه | هدف | نیاز به مشتق‌پذیری | تابع‌های فعال‌سازی مناسب | مشکلات مرتبط |
| :--- | :--- | :--- | :--- | :--- |
| **Feed Forward** | ایجاد غیرخطی بودن و درک پیچیدگی (Complex Data) | خیر | Linear (ضعیف), Sigmoid, Tanh, ReLU, Leaky ReLU, ELU | استفاده از Linear منجر به کاهش پتانسیل آموزشی می‌شود. |
| **Backpropagation** | انتشار خطا و محاسبه گرادیان برای به‌روزرسانی وزن‌ها | **بله** (تابع باید مشتق‌پذیر باشد) | **ترجیحاً:** ReLU و زیرشاخه‌های آن | **Vanishing Gradient** (Sigmoid, Tanh), **Dying ReLU** (ReLU) |
| **Hidden Layers** | غیرخطی کردن و یادگیری ویژگی‌ها | بله | **ReLU**, Leaky ReLU, ELU | |
| **Output Layer** | ارائه خروجی نهایی متناسب با نوع مسئله | بله | **Regression:** Linear | |
| | | | **Binary Classification:** Sigmoid | |
| | | | **Multi-class Classification:** Softmax | |

**نکته مهم در انتخاب:** درک این مفاهیم به ما کمک می‌کند تا در زمان بروز مشکلاتی مانند کندی آموزش یا عدم افزایش دقت (Accuracy)، به جای سنگین‌تر کردن بیهوده شبکه، به سراغ بررسی و تغییر **تابع فعال‌سازی** برویم که یک **هایپرپارامتر** حیاتی است.

---

