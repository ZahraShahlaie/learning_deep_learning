بسیار عالی! در ادامه، خلاصه کامل و جزئیات مباحثی که در ویدیو مطرح شد را با ساختاردهی مناسب، عینا مطابق با روند توضیحات شما، ارائه می‌دهم.

---

# 🧠 بهینه‌سازها (Optimizers) در شبکه‌های عصبی

**تعریف:** یک بهینه‌ساز در شبکه‌ عصبی، الگوریتمی است که برای **تنظیم کردن یا بهبود وزن‌ها و بایاس‌های** شبکه به‌منظور کاهش تابع زیان (Loss Function) استفاده می‌شود.

این تعریف شما را به یاد **گرادیان دیسنت (Gradient Descent)** می‌اندازد که نقش اصلی اپتیمایزر را در الگوریتم یادگیری بازی می‌کند.

## ۱. گرادیان دیسنت (Gradient Descent - GD)

### ۱.۱. ضعف‌های اساسی گرادیان دیسنت
1.  **استفاده از کل دیتاست:** GD کل دیتاست را یکجا نگاه می‌کند، گرادیان را محاسبه می‌کند و سپس وزن‌ها را آپدیت می‌کند.
    * **محدودیت حافظه:** برای دیتاست‌های بزرگ، محدودیت مموری جدی ایجاد می‌کند.
    * **کندی آپدیت:** برای آپدیت وزن‌ها باید منتظر بمانیم تا کل دیتاست دیده شود.

## ۲. استوکاستیک گرادیان دیسنت (Stochastic Gradient Descent - SGD)

برای حل ضعف‌های GD، ایده **Mini-Batch** مطرح شد.

* **SGD چیست؟** همان گرادیان دیسنت است، با این تفاوت که به‌جای استفاده از کل دیتاست، از **مینی‌بچ‌ها** استفاده می‌کند. در هر تکرار، یک مینی‌بچ برداشته می‌شود و شبکه با آن آموزش داده می‌شود.

### ۲.۱. مشکلات مینی‌بچ‌ها
* **عدم توازن کلاس‌ها:** اگر مینی‌بچ‌ها به‌طور تصادفی شامل تعداد نامتوازنی از نمونه‌های کلاس‌های مختلف باشند (مثلاً MNIST: اعداد ۰ تا ۹)، شبکه ممکن است روی کلاس غالب بیشتر تمرکز کند و دچار **سوگیری (Bias)** شود.
* **راه‌حل:** برای جلوگیری از سوگیری، بهتر است مینی‌بچ‌ها **به‌طور تصادفی (Stochastic)** از دیتاست انتخاب شوند.

### ۲.۲. مشکل اساسی مشترک (GD و SGD)
* هر دو الگوریتم همچنان با مشکل گیر کردن در **مینیمم محلی (Local Minimum)** مواجه هستند و نمی‌توانند به نقطه بهینه جهانی (Global Minimum) دست یابند.

---

## ۳. مومنتوم (Momentum)

برای حل مشکل گیر کردن در مینیمم‌های محلی، ایده **مومنتوم (تکانه)** مطرح شد.

* **ایده:** فرض کنید توپی از یک سطح رها می‌شود. توپی که به یک فرورفتگی محلی می‌رسد، به دلیل سرعت و مسیری که طی کرده است (تکانه)، می‌تواند از آن عبور کرده و به سمت مینیمم‌های عمیق‌تر حرکت کند.

### ۳.۱. پیاده‌سازی مومنتوم
برای پیاده‌سازی این ایده، یک **حافظه** در گرادیان ایجاد می‌کنیم تا **گرادیان‌های قبلی** را در محاسبات لحاظ کند.

* **فرمول مومنتوم (سنتز شده در ویدیو):**
    برای آپدیت وزن ($W$):
    $$W_{i+1} = W_i - \eta \cdot (\frac{\partial L}{\partial W_i} + \alpha \cdot \frac{\partial L}{\partial W_{i-1}})$$
    * **نکته توضیحی شما:** در واقع، به‌جای اینکه فقط گرادیان لحظه‌ای را لحاظ کنیم، عامل حرکت قبلی را نیز در نظر می‌گیریم. (این کار با محاسبه سرعت $V$ در فرمول استاندارد مومنتوم انجام می‌شود).
* **$\alpha$ (ضریب آلفا):** یک هایپرپارامتر است که تعیین می‌کند **مقدار ذخیره تاریخچه (حافظه)** چقدر باشد.
    * اگر $\alpha = 0$، به همان الگوریتم SGD برمی‌گردیم.
* **مزیت:** مومنتوم به ما کمک می‌کند که از مناطق لوکال عبور کرده و به نقطه کمینه بهتری دست پیدا کنیم.

---

## ۴. مشکل نرخ یادگیری (Learning Rate - $\eta$)

یکی از مسائل مهم در بهینه‌سازی، تعیین مقدار **نرخ یادگیری ($\eta$)** است.

* **قانون کلی:** نرخ یادگیری باید "Small Enough and Big Enough" باشد.
    * **اگر $\eta$ خیلی کم باشد:** آموزش با شیب بسیار کمی پیش می‌رود و رسیدن به نقطه بهینه زمان بسیار زیادی (ایپاک‌های بالا) می‌برد.
    * **اگر $\eta$ خیلی زیاد باشد:** شبکه اصلاً نمی‌تواند آموزش ببیند، مقدار زیان (Loss) افزایش پیدا می‌کند و با پرش‌های بزرگ از اپتیموم دور می‌شود.
    * **اگر $\eta$ بالا باشد (اما نه خیلی زیاد):** در ابتدا جهش سریع و خوبی در یادگیری داریم (به‌سرعت به نزدیکی اپتیموم می‌رسیم)، اما پس از آن به دلیل جهش‌های بزرگ، اطراف نقطه اپتیموم نوسان می‌کند و نمی‌تواند به آن همگرا شود.

**حالت بهینه (Learning Rate Scheduling):**
* در ابتدا، $\eta$ باید **زیاد** باشد (برای حرکت سریع به سمت اپتیموم).
* سپس، $\eta$ باید **کاهش** یابد (برای گام‌های کوچک و همگرایی دقیق به اپتیموم).
* **هدف:** دستیابی به دقت بهتر با کمترین میزان مصرف منابع سخت‌افزاری و زمانی (اقتصادی کردن فرآیند آموزش).

### ۴.۱. راه‌حل‌های نرخ یادگیری داینامیک

#### الف. برنامه‌ریزی نرخ یادگیری (Learning Rate Schedule)
* **کاهش نمایی (Exponential Decay):** تغییر نرخ یادگیری به‌صورت دستی با یک رابطه از پیش تعیین شده.
    $$\eta_i = \eta_0 \cdot e^{-k \cdot i}$$
    * $\eta_i$: نرخ یادگیری در اپوک $i$.
    * $k$: هایپرپارامتری که شدت کاهش را تعیین می‌کند.

#### ب. نرخ یادگیری تطبیقی (Adaptive Learning Rate)

این روش‌ها به‌صورت هوشمند و بر اساس گرادیان، نرخ یادگیری را به‌طور داینامیک تنظیم می‌کنند:

**آداگراد (AdaGrad)**

* **ایده:** گرادیان به صورت مستقیم در تعیین نرخ یادگیری اثر می‌گذارد. اگر گرادیان زیاد باشد، $\eta$ بالا نگه داشته می‌شود؛ اگر گرادیان کم شود (نزدیک شدن به اپتیموم)، $\eta$ پایین آورده می‌شود.
* **رابطه انباشت گرادیان ($G_i$):**
    $$G_i = G_{i-1} + \left( \frac{\partial L}{\partial W_i} \right)^2$$
* **رابطه به‌روزرسانی وزن ($W_{i+1}$):**
    $$W_{i+1} = W_i - \frac{\eta}{\sqrt{G_i + \epsilon}} \cdot \frac{\partial L}{\partial W_i}$$
    * **$\epsilon$ (اپسیلون):** مقداری بسیار کوچک ($10^{-7}$ یا $10^{-8}$) که برای جلوگیری از تقسیم بر صفر (زمانی که $G_i$ صفر می‌شود) اضافه می‌شود.

**RMSprop (Root Mean Square Propagation)**

* **هدف:** مشابه آداگراد، اما نحوه آپدیت $G$ متفاوت است تا از کاهش سریع و دائم نرخ یادگیری جلوگیری شود.
* **رابطه انباشت گرادیان ($G_i$):**
    $$G_i = \beta \cdot G_{i-1} + (1-\beta) \cdot \left( \frac{\partial L}{\partial W_i} \right)^2$$
    * از یک ضریب $\beta$ (معمولاً $0.9$) برای کاهش اثر گرادیان‌های خیلی قدیمی استفاده می‌شود.
* **رابطه به‌روزرسانی وزن ($W_{i+1}$):**
    $$W_{i+1} = W_i - \frac{\eta}{\sqrt{G_i + \epsilon}} \cdot \frac{\partial L}{\partial W_i}$$

---

## ۵. آدام (Adam - Adaptive Moment Estimation)

**ضعف AdaGrad و RMSprop:** این دو الگوریتم ویژگی **مومنتوم** را داخل خود ندارند.

* **ایده آدام:** ترکیب بهترین‌های RMSprop (نرخ یادگیری تطبیقی) و مومنتوم.
    $$\text{Adam} = \text{RMSprop} + \text{Momentum}$$

* **رابطه نهایی آدام (سنتز شده در ویدیو):**
    به‌جای استفاده از گرادیان لحظه‌ای در مخرج RMSprop، از رابطه مومنتوم استفاده می‌کنیم:
    $$W_{i+1} = W_i - \frac{\eta}{\sqrt{G_i + \epsilon}} \cdot \left( \frac{\partial L}{\partial W_i} + \alpha \cdot \frac{\partial L}{\partial W_{i-1}} \right)$$

* **مزیت:** آدام به دلیل داشتن هم **مومنتوم** و هم قابلیت **تنظیم داینامیک نرخ یادگیری**، نسبت به اغلب الگوریتم‌های دیگر، عملکرد بهینه‌تر و مناسب‌تری دارد و به‌طور کلی قابل قبول‌تر است.

---
اگر تمایل دارید، می‌توانم یک کد پایتون ساده برای مقایسه بصری حرکت چند بهینه‌ساز مختلف در فضای زیان (Loss Space) برای شما پیدا و ارائه کنم.
