حتماً، در ادامه خلاصه‌ای کامل و دقیق از محتوای ارائه شده در مورد شبکه‌های عصبی، پرسپترون‌ها، لایه‌ها، وزن‌ها، بایاس، و تابع فعال‌سازی (Activation Function) آمده است.

---

## 🧠 شبکه‌های عصبی و پرسپترون (Neural Networks and Perceptrons)

### نورون و پرسپترون
* **نورون (Neuron):** به واحد پردازشی در شبکه عصبی گفته می‌شود که از نام نورون‌های مغز انسان گرفته شده است.
* **پرسپترون (Perceptron):** به کل ساختار یک واحد که شامل ورودی‌ها، وزن‌ها، محاسبات جمع وزنی و یک خروجی است، پرسپترون گفته می‌شود.

* <img width="771" height="266" alt="image" src="https://github.com/user-attachments/assets/d9b5dabf-3575-4e58-b8d0-d8616983dac2" />


### 🧮 محاسبه جمع وزنی (Weighted Sum)
خروجی یک نورون (واحد پردازشی) از طریق محاسبه **جمع وزنی** ورودی‌ها تعیین می‌شود.
اگر $X$ یک بردار ورودی با ویژگی‌های $x_0, x_1, \dots$ و $W$ بردار وزن‌های متناظر $w_0, w_1, \dots$ باشد، جمع وزنی به صورت زیر محاسبه می‌شود:
$$\text{Weighted Sum} = (w_0 \cdot x_0) + (w_1 \cdot x_1) + \dots + (\text{Bias})$$
* **وزن‌ها ($W$):** برای هر ورودی یک وزن (Weight) اختصاص داده می‌شود. این وزن‌ها در فرآیند آموزش شبکه، به‌روزرسانی و بهینه‌سازی می‌شوند.
* **بایاس ($B$):** یک مقدار ثابت است که به جمع وزنی اضافه می‌شود.
<img width="1149" height="415" alt="image" src="https://github.com/user-attachments/assets/e7de58bd-d2e6-4fea-b169-5028c0bff196" />
<img width="585" height="283" alt="image" src="https://github.com/user-attachments/assets/a268e4af-c607-4dce-bcf5-26b01a1a3623" />
<img width="1069" height="288" alt="image" src="https://github.com/user-attachments/assets/529e8b1c-c7f6-42f9-b28b-7628bf649cfa" />

<img width="956" height="426" alt="image" src="https://github.com/user-attachments/assets/26b390d1-4150-43af-9742-d271e91d1908" />


---

## 🏗️ ساختار لایه‌ها در شبکه عصبی (Layer Structure)

یک شبکه عصبی از چندین لایه تشکیل شده است:

1.  **لایه ورودی (Input Layer):** شامل گره‌هایی به تعداد ویژگی‌های ورودی ($x_1, x_2, x_3, \dots$).
2.  **لایه‌های مخفی (Hidden Layers):** لایه‌های میانی که محاسبات اصلی شبکه در آن‌ها انجام می‌شود.
3.  **لایه خروجی (Output Layer):** لایه‌ای که نتیجه نهایی را تولید می‌کند.

### 📊 نمایش ماتریسی محاسبات
در یک لایه، محاسبه جمع وزنی برای تمامی نورون‌های لایه را می‌توان به صورت ماتریسی نمایش داد.
برای یک مثال با **۳ ورودی** ($X_1, X_2, X_3$) و **۴ نورون** در لایه مخفی، محاسبات به شکل زیر خواهد بود:






<img width="260" height="68" alt="image" src="https://github.com/user-attachments/assets/13a31a46-2933-4c81-868f-4db200028a42" />


<img width="809" height="423" alt="image" src="https://github.com/user-attachments/assets/b7694e8f-b25b-40ff-a844-7d8bc33f438b" />

<img width="905" height="322" alt="image" src="https://github.com/user-attachments/assets/7cb6a892-9cd8-4ed9-8acb-d327bfe561ad" />


---

## ⚖️ بایاس (Bias)

* **نقش اول (عرض از مبدأ):** بایاس همانند **عرض از مبدأ** در رگرسیون خطی عمل می‌کند و به مدل اجازه می‌دهد تا بدون در نظر گرفتن ورودی‌ها، یک مقدار ثابت را تولید کند.
* **نقش دوم (مقابله با سوگیری/نادیده گرفتن صفر):** بایاس از **سوگیری (Bias)** مدل به سمت گروه‌های خاصی از مقادیر جلوگیری می‌کند و همچنین مانع از **نادیده گرفته شدن مقادیر صفر معنی‌دار** در ورودی‌ها می‌شود.
    * اگر مقدار ویژگی ورودی ($x_i$) برابر صفر باشد، حاصل ضرب آن در وزن متناظرش ($w_i \cdot x_i$) همواره صفر خواهد بود. در این حالت، بایاس تضمین می‌کند که حتی در صورت صفر بودن ورودی‌ها، نورون بتواند یک خروجی غیر صفر تولید کند و تأثیر آن ویژگی نادیده گرفته نشود.

<img width="922" height="514" alt="image" src="https://github.com/user-attachments/assets/0204c785-a1cb-4a30-af08-144cee45f44f" />

---

## 📈 تابع فعال‌سازی (Activation Function)

**تابع فعال‌سازی** ($f$) به خروجی جمع وزنی ($Z$) هر نورون اعمال می‌شود:
$$\text{Output} = f(Z) = f(\mathbf{X} \cdot \mathbf{W} + \mathbf{B})$$

<img width="877" height="275" alt="image" src="https://github.com/user-attachments/assets/e1f1cdeb-efe9-4074-8968-87ad4a8438df" />
<img width="934" height="460" alt="image" src="https://github.com/user-attachments/assets/4cf8200a-a09a-4483-8e83-0edecb2eecd9" />


### اهمیت تابع فعال‌سازی
1.  **اضافه کردن خاصیت غیرخطی (Non-Linearity):** محاسبه جمع وزنی یک فرآیند **خطی (Linear)** است. اگر از تابع فعال‌سازی استفاده نشود یا از یک تابع خطی استفاده شود، صرف نظر از تعداد لایه‌ها، کل شبکه در نهایت یک مدل خطی خواهد بود. استفاده از توابع فعال‌سازی **غیرخطی** (مانند Sigmoid، ReLU و ...) به شبکه این امکان را می‌دهد که **روابط پیچیده‌تر و غیرخطی** بین داده‌ها را بیاموزد.
2.  **نرمال‌سازی خروجی (Normalization):** برخی از توابع فعال‌سازی (مانند Sigmoid) خروجی را در یک محدوده مشخص (مانند $[0, 1]$) نرمال‌سازی می‌کنند. این کار از بزرگ شدن یا کوچک شدن بیش از حد مقادیر خروجی جلوگیری کرده و به پایداری فرآیند آموزش کمک می‌کند.

### مثال: تابع سیگموئید (Sigmoid)
* **فرمول:** $f(z) = \frac{1}{1 + e^{-z}}$
* **ویژگی:** خروجی این تابع همیشه بین **صفر و یک** است، که به عنوان یک ابزار نرمال‌سازی در شبکه عمل می‌کند.
<img width="1082" height="356" alt="image" src="https://github.com/user-attachments/assets/b4bb802a-c8ab-47ea-a64f-e448aeb05a54" />


### هایپرپارامتر
تابع فعال‌سازی یکی از **هایپرپارامترهای** مدل است که باید بر اساس نوع مسئله (مثل رگرسیون، طبقه‌بندی) و ویژگی‌های داده، به‌درستی انتخاب و تنظیم شود.

---

## 🔄 آموزش شبکه عصبی (Training the Neural Network)

### فرآیند اولیه
1.  **مقداردهی اولیه وزن‌ها:** در شروع فرآیند آموزش، تمام وزن‌ها در تمام لایه‌ها به صورت **تصادفی (Randomly)** مقداردهی اولیه می‌شوند.
2.  **محاسبه خطا:** خروجی شبکه با خروجی واقعی مقایسه شده و یک مقدار خطا (Error) یا زیان (Loss) توسط **تابع زیان (Loss Function)** یا **تابع هزینه (Cost Function)** محاسبه می‌شود.

### به‌روزرسانی وزن‌ها
* **الگوریتم انتشار به عقب (Backpropagation):** برای به‌روزرسانی و **بهینه‌سازی (Optimization)** وزن‌ها از الگوریتم بک‌پروپگیشن استفاده می‌شود. این فرآیند بر اساس خطای محاسبه شده، وزن‌ها را به صورت مداوم تنظیم می‌کند تا خطا کاهش یابد.

### جعبه سیاه (Black Box)
* شبکه‌های عصبی بزرگ اغلب به عنوان **جعبه سیاه (Black Box)** شناخته می‌شوند.
* **توجیه رفتار:** اگرچه ما الگوریتم‌های آموزش (مانند بک‌پروپگیشن) را می‌شناسیم، اما توجیه اینکه مدل دقیقاً **چه چیزی** را در لایه‌های میانی آموخته و چرا رفتار خاصی از خود نشان می‌دهد، همیشه ممکن نیست.
* **هدف اصلی:** هدف از یادگیری، درک فرآیند آموزش (ترین)، بهینه‌سازی (اپتیمایز)، و تنظیم درست **هایپرپارامترها** است تا مدل به درستی عمل کند و بتوان مشکلاتی مانند انفجار گرادیان یا محوشدگی گرادیان را مدیریت کرد.

---

