بسیار خوب، بر اساس محتوای ارائه شده در مورد شبکه‌های عصبی کانولوشنال (CNN) و نحوه کارکرد کانولوشن‌ها (فیلترها)، در اینجا یک **جزوه جامع، دقیق و کامل** آورده شده است.

---

## 📝 جزوه جامع: شبکه‌های عصبی کانولوشنال (CNN) و عملیات کانولوشن

### ۱. مقایسه CNN و شبکه‌های Fully Connected (FC)

| ویژگی | شبکه‌های Fully Connected (FC) | شبکه‌های Convolutional (CNN) |
| :--- | :--- | :--- |
| **ورودی تصویر** | تصویر (ماتریس) باید ابتدا با عملیات **Flatten** به یک **بردار یک‌بُعدی** تبدیل شود. | تصویر (ماتریس) به صورت **یکپارچه (سه‌بُعدی)** حفظ می‌شود. |
| **نگرش به داده** | **پیکسل به پیکسل** و **گلوبال**؛ موقعیت فضایی پیکسل‌ها (همسایگی) از دست می‌رود. | **یکپارچه** و **محلی (Local)**؛ روابط فضایی بین پیکسل‌ها حفظ می‌شود. |
| **استخراج ویژگی** | وزن‌ها به صورت جداگانه هر پیکسل را دریافت می‌کنند. | از **فیلترها (کرنل‌ها)** برای استخراج ویژگی‌های محلی استفاده می‌شود. |
| **مشکل اصلی** | با بزرگ شدن ابعاد تصویر، **تعداد پارامترها به شدت افزایش می‌یابد** (مقیاس‌پذیری ضعیف). | به دلیل **اشتراک‌گذاری وزن** و **اتصالات محلی**، تعداد پارامترها بسیار کم است (مقیاس‌پذیری قوی). |

---

### ۲. فلسفه و عملکرد کانولوشن (فیلتر/کرنل)

**فلسفه اصلی CNN:** CNN به جای اینکه تصویر را خرد کند و متغیرها را جدا ببیند، تصویر را یکپارچه می‌بیند و برای این کار از **فیلترها (کرنل‌ها)** استفاده می‌کند.

#### A. تعریف فیلتر و کرنل

* **کرنل (Kernel) یا فیلتر (Filter):** یک ماتریس کوچک (معمولاً $3 \times 3$ یا $5 \times 5$) که مقادیر آن **وزن‌های** قابل یادگیری شبکه هستند.
* **عملکرد:** فیلتر روی قسمت‌های مختلف تصویر حرکت می‌کند و عملیات **استخراج ویژگی (Feature Extraction)** را انجام می‌دهد.
* **مانند فیلترهای ویرایش تصویر:** همانند فیلترهایی که در نرم‌افزارهایی مثل فتوشاپ برای شارپ کردن، بلر کردن یا تشخیص لبه‌ها (Edge Detection) استفاده می‌شوند، فیلترهای CNN نیز ویژگی‌های بصری را استخراج می‌کنند.

#### B. مکانیزم عملیات کانولوشن

عملیات کانولوشن شامل مراحل زیر است:

1.  **برداشت بخش محلی:** کرنل (مثلاً $3 \times 3$) روی یک ناحیه از تصویر (به همان ابعاد $3 \times 3$) قرار می‌گیرد.
2.  **ضرب نظیر به نظیر (Element-wise Multiplication):** هر عنصر (وزن) در ماتریس کرنل در پیکسل متناظر خود در تصویر ضرب می‌شود.
3.  **جمع‌بندی:** تمام مقادیر حاصل از ضرب با هم جمع می‌شوند.
4.  **تولید Feature Map:** عدد حاصل از جمع، در یک پیکسل از ماتریس خروجی (به نام **Feature Map** یا **Activation Map**) قرار می‌گیرد.
5.  **حرکت (Sliding):** کرنل به صورت گام‌به‌گام (با توجه به Stride) روی کل تصویر حرکت کرده و این عملیات تکرار می‌شود تا Feature Map تکمیل شود. 

#### C. کرنل در تصاویر رنگی (سه‌بُعدی)

* **تصویر ورودی رنگی (RGB):** دارای سه لایه عمقی (Red، Green، Blue) است. (مثلاً $N \times N \times 3$).
* **ابعاد کرنل:** کرنل نیز باید دارای **همان عمق ورودی** باشد. (مثلاً $3 \times 3 \times 3$).
* **محاسبه:** سه کرنل $3 \times 3$ (یکی برای هر لایه رنگ) به صورت موازی با لایه‌های ورودی ضرب می‌شوند و نتایج حاصل از هر سه لایه **در نهایت با هم جمع** می‌شوند تا **فقط یک لایه** در Feature Map تولید شود.
* **خروجی:** یک کرنل تکی (با هر عمقی) همیشه یک Feature Map **تک‌لایه** تولید می‌کند.

---

### ۳. استخراج ویژگی‌های خودکار و مزیت اصلی CNN

#### A. یادگیری فیلترها (کرنل‌های Trainable)

* **تغییر پارادایم:** در روش‌های پردازش تصویر قدیمی، فیلترها (مثل فیلتر Edge Detection) به صورت **دستی** توسط انسان تعریف می‌شدند.
* **ایده CNN:** در شبکه‌های CNN، مقادیر داخل کرنل به جای اینکه مقادیر ثابت و پیش‌فرض باشند، **وزن‌های یادگیری (Trainable Weights)** هستند.
* **مکانیزم یادگیری:** این وزن‌ها با استفاده از الگوریتم‌های بهینه‌سازی (مانند **Backpropagation**) در طول آموزش به‌روزرسانی می‌شوند تا **به صورت خودکار** مهم‌ترین ویژگی‌های تصویر (مانند لبه‌ها، بافت‌ها، و در لایه‌های عمیق‌تر، اجزای پیچیده مانند چشم یا گوش) را استخراج کنند.

#### B. مزیت اول: کاهش چشمگیر پارامترها

CNN مشکل مقیاس‌پذیری FC را حل می‌کند:

1.  **استقلال از ابعاد تصویر:** تعداد پارامترهای CNN فقط به **اندازه فیلتر** و **عمق ورودی** و **تعداد فیلترها** بستگی دارد، نه به اندازه $N \times N$ تصویر.
2.  **اشتراک‌گذاری وزن (Weight Sharing):** یک مجموعه کوچک از وزن‌ها (فیلتر) برای پردازش **تمام نقاط** تصویر استفاده می‌شود.

> **مثال مقایسه‌ای (تصویر $1000 \times 1000 \times 3$):**
> * **FC:** می‌تواند به میلیاردها پارامتر برسد.
> * **CNN (با ۱۰ کرنل $3 \times 3$):** تنها $10 \times ((3 \times 3 \times 3) + 1) = 280$ پارامتر دارد.

#### C. مزیت دوم: حفظ روابط محلی (Location Invariance)

* **مشکل FC:** در FC، جابه‌جا شدن یک شیء در تصویر (مثلاً گربه در گوشه بالا باشد یا در مرکز) باعث تغییر شدید در پیکسل‌های ورودی می‌شود و شبکه باید دوباره آن الگو را از نو یاد بگیرد.
* **مزیت CNN:** به دلیل **اتصالات محلی** و **اشتراک‌گذاری وزن**، فیلترها الگوها را در هر نقطه‌ای از تصویر شناسایی می‌کنند. این باعث می‌شود CNN نسبت به **جابه‌جایی شیء** (Translation Invariance) قوی‌تر عمل کند.

---

### ۴. ساختار سلسله مراتبی و Feature Mapها

#### A. استفاده از چندین کرنل

* **هدف:** برای اینکه شبکه بتواند ویژگی‌های بیشتری (مثل لبه‌های عمودی، لبه‌های افقی، بافت‌ها و غیره) را به طور همزمان استخراج کند، از **چندین کرنل** به صورت موازی استفاده می‌شود.
* **تولید Feature Map:** اگر یک لایه کانولوشن از $K$ کرنل استفاده کند، خروجی آن یک Feature Map **چند لایه** با عمق $K$ خواهد بود.

#### B. ترکیب لایه‌ها

* **Feature Map به عنوان ورودی بعدی:** Feature Map خروجی یک لایه کانولوشن به عنوان ورودی (با عمق جدید) برای لایه کانولوشن بعدی استفاده می‌شود.
* **سلسله مراتب:** با عمیق‌تر شدن شبکه (افزایش تعداد لایه‌ها)، فیلترها یاد می‌گیرند که:
    * **لایه‌های کم‌عمق:** ویژگی‌های ساده (لبه‌ها، رنگ‌ها) را استخراج کنند.
    * **لایه‌های عمیق:** ویژگی‌های ساده را ترکیب کرده و ویژگی‌های پیچیده و انتزاعی‌تر (مانند ساختار کلی شیء) را استخراج کنند.

---

### ۵. پارامترهای لایه Batch Normalization (BN)

* **لایه BN:** لایه BN دارای پارامترهای قابل آموزش است.
* **فرمول پارامترهای BN:** $\mathbf{2 \times \text{تعداد کانال‌های ورودی}}$
    * $\mathbf{\gamma}$ (گاما): برای مقیاس‌دهی (Scaling).
    * $\mathbf{\beta}$ (بتا): برای شیفت (Shifting).

این پارامترها **قابل آموزش (Trainable)** هستند، اما لایه‌هایی مثل Pooling و Dropout فاقد پارامترهای یادگیری هستند.
