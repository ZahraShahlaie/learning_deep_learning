لینک ویدیو:
https://youtu.be/KjDP7Y9poEc?si=FrDd-iQB6rXHUGMV

در اینجا یک جزوه کامل، دقیق و جامع بر اساس توضیحات ارائه شده در مورد ساختار و عملکرد عمیق‌تر شبکه‌های عصبی (Neural Networks) آورده شده است.

# 🧠 کارکرد عمیق‌تر شبکه‌های عصبی

## ۱. 🔬 شبیه‌سازی یک نورون با رگرسیون خطی

کارکرد اساسی هر **نورون** در شبکه عصبی مشابه همان مثالی است که در مبحث بهینه‌سازی (Optimization) با استفاده از **گرادیان کاهشی (Gradient Descent)** برای حل مسئله **رگرسیون خطی (Linear Regression)** حل شد.

### ساختار نورون تکی:

* **ورودی و خروجی:** یک نورون ورودی‌هایی (ویژگی‌ها یا $X_i$) را می‌گیرد و یک خروجی ($Y$) تولید می‌کند.
<img width="895" height="413" alt="image" src="https://github.com/user-attachments/assets/2f3bfa4e-c4bb-462f-b544-3a26a19fd0fc" />
<img width="895" height="537" alt="image" src="https://github.com/user-attachments/assets/5ab80721-f731-4219-ba19-7ec3e34300ba" />

* **رابطه ریاضیاتی:** هر نورون به تنهایی یک **رگرسیون خطی ساده** را حل می‌کند. این رابطه برای $N$ ورودی به صورت زیر است:

    $$\hat{Y} = W_0 X_0 + W_1 X_1 + \dots + W_n X_n + B$$
    * **$X_i$:** ویژگی‌های ورودی (مانند $X_0, X_1, X_2$).
    * **$W_i$:** ضرایب متغیر ورودی‌ها، که در نورال نتورک به آن‌ها **وزن (Weight)** گفته می‌شود.
    * **$B$:** مقدار ثابت معادله خطی، که در نورال نتورک به آن **بایاس (Bias)** گفته می‌شود.
    * **$Y$:** مقدار خروجی پیش‌بینی شده ($\hat{Y}$).

    <img width="1070" height="554" alt="image" src="https://github.com/user-attachments/assets/4426c64f-8554-4b87-ab5a-27958da3906b" />

* **نحوه بهینه‌سازی (آموزش):**
    1.  ابتدا، **وزن‌ها ($W$) و بایاس ($B$)** به صورت **تصادفی (Random)** مقداردهی اولیه می‌شوند.
    2.  مقدار خروجی ($Y$) با استفاده از ورودی‌ها و وزن‌های فعلی محاسبه می‌شود (مسیر **پیشروی**).
    3.  **خطا (Error)** یا **تابع زیان (Loss Function)** محاسبه می‌شود.
    4.  **گرادیان (مشتق خطا نسبت به وزن‌ها و بایاس)** محاسبه می‌شود.
    5.  وزن‌ها و بایاس بر اساس گرادیان و نرخ یادگیری **آپدیت (Update)** می‌شوند (مسیر **پس انتشار**).
* **هدف آموزش (Training):** در فرآیند آموزش، **وزن‌ها ($W$) و بایاس ($B$)** آپدیت می‌شوند تا به یک مقدار **بهینه (Optimum)** برسیم و مقدار **خطا (Error)** را کاهش دهیم.

### ⚠️ نکته تکمیلی: تابع فعال‌سازی (Activation Function)
تنها تفاوت نورون مصنوعی با یک مدل رگرسیون خطی ساده، استفاده از **تابع فعال‌سازی** است که خروجی خطی را به یک خروجی غیرخطی تبدیل می‌کند. (این مفهوم در جلسات بعدی به تفصیل توضیح داده خواهد شد).

---

## ۲. 📐 نمایش با جبر خطی (Linear Algebra)

برای کار با چندین ورودی و خروجی در یک نورون یا لایه، از ضرب ماتریسی استفاده می‌شود که پیاده‌سازی رابطه خطی را ساده می‌کند:

### رابطه کلی جبر خطی:

<img width="676" height="610" alt="image" src="https://github.com/user-attachments/assets/cdb7e494-e6fe-499f-88d6-59f7af59f768" />
<img width="934" height="503" alt="image" src="https://github.com/user-attachments/assets/2ea7fde1-17e8-43a6-8b04-de0c41d03e4e" />
<img width="863" height="591" alt="image" src="https://github.com/user-attachments/assets/123aab99-d89c-40e0-b6aa-8030e86da9c0" />




$Y = X \cdot W + B$

* **حالت چند ورودی و یک خروجی (تک نورون):**
    * **$X$** (بردار ورودی‌ها): $1 \times M$ (یک سطر و $M$ ویژگی).
    * **$W$** (ماتریس وزن‌ها): $M \times 1$ (به تعداد ورودی‌ها و یک خروجی).
    * **$B$** (بایاس): $1 \times 1$ (یک مقدار).
    * **$Y$** (خروجی): $1 \times 1$ (یک مقدار خروجی).
    * **نتیجه ضرب:** $(1 \times M) \cdot (M \times 1) = 1 \times 1$.

* **حالت چند ورودی و چند خروجی (یک لایه):**
    * **$X$** (بردار ورودی‌ها): $1 \times M$ ($M$ تعداد ورودی).
    * **$W$** (ماتریس وزن‌ها): $M \times N$ ($M$ ورودی و $N$ خروجی).
    * **$B$** (بردار بایاس): $1 \times N$ (بایاس به تعداد خروجی‌ها).
    * **$Y$** (بردار خروجی‌ها): $1 \times N$ (یک سطر و $N$ خروجی).
    * **نتیجه ضرب:** $(1 \times M) \cdot (M \times N) + (1 \times N) = 1 \times N$.
<img width="1091" height="356" alt="image" src="https://github.com/user-attachments/assets/92a805e8-1b46-47c0-8746-2e4a617d546d" />

---

## ۳. 🔗 ساختار شبکه عصبی کامل (Neural Network)

شبکه عصبی با قرار دادن چندین نورون در لایه‌های پی در پی ساخته می‌شود. 



### اجزای اصلی شبکه:

#### ۱. لایه ورودی (Input Layer)
* **وظیفه:** دریافت ویژگی‌های خام ورودی ($X$).
* **اندازه:** تعداد نورون‌های این لایه **دقیقا** برابر با **تعداد ویژگی‌های ورودی (Features)** است.

#### ۲. لایه‌های پنهان (Hidden Layers)
* **وظیفه:** انجام اصلی‌ترین محاسبات و تبدیل‌های غیرخطی برای درک الگوها.
* **ساختار:** خروجی هر نورون در یک لایه، به عنوان ورودی به **تمام** نورون‌های لایه بعدی داده می‌شود (شبکه **کاملاً متصل - Fully Connected**).
* **اندازه:** **تعداد لایه‌ها** و **تعداد نورون‌ها در هر لایه** یک **هایپرپارامتر** است و مقدار مشخصی ندارد. این اندازه‌ها بر اساس جنس داده، پیچیدگی مسئله و نوع شبکه تعیین می‌شوند.

#### ۳. لایه خروجی (Output Layer)
* **وظیفه:** تولید نتیجه نهایی شبکه (پیش‌بینی).
* **اندازه:** تعداد نورون‌های این لایه برابر است با **تعداد کلاس‌ها** (در مسائل دسته‌بندی - Classification) یا **تعداد خروجی‌های مورد نیاز** (در مسائل رگرسیون).
    * **مثال:** اگر مسئله دسته‌بندی اعداد $0$ تا $9$ (MNIST) باشد، لایه خروجی **۱۰** نورون خواهد داشت.

<img width="1053" height="574" alt="image" src="https://github.com/user-attachments/assets/63161ec1-6fc2-4aef-8799-53ace452012a" />


### خلاصه فرآیند یادگیری در شبکه:

1.  **پیشروی (Feedforward):** ورودی‌ها از لایه اول به لایه‌های پنهان و در نهایت به لایه خروجی منتقل می‌شوند و نتیجه محاسبه می‌گردد.
2.  **محاسبه خطا (Loss):** خطا بین خروجی مدل و مقدار واقعی محاسبه می‌شود.
3.  **پس انتشار (Backpropagation):** گرادیان‌ها محاسبه شده و وزن‌ها و بایاس‌های تمام اتصالات شبکه **آپدیت** می‌شوند تا خطا به حداقل برسد و مدل به حالت **بهینه** برسد.

