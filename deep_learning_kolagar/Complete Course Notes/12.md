این یک جزوه کامل، دقیق و جامع است که تمام نکات کلیدی ارائه شده در مورد شبکه‌های کانولوشنال (CNN) و اجزای آن‌ها را در بر می‌گیرد:

---

## 📝 جزوه کامل: ساختار و هدف شبکه‌های عصبی کانولوشنال (CNN)

### ۱. هدف و ساختار کلی CNN

| بخش | توضیحات و وظیفه |
| :--- | :--- |
| **هدف اصلی CNN** | انجام **Feature Extraction (استخراج ویژگی)** از تصاویر ورودی. |
| **Input Layer (لایه ورودی)** | تصویر (ماتریکس) مستقیماً به شبکه داده می‌شود (مثلاً $227 \times 227 \times 3$ برای RGB)، برخلاف شبکه‌های Dense که به آن‌ها یک بردار یک‌بُعدی (Flattened) داده می‌شود. |
| **Convolutional Layers (لایه‌های Conv)** | وظیفه اصلی **استخراج ویژگی‌ها** از تصویر. این لایه‌ها می‌توانند به صورت **متعدد و پشت سر هم** قرار بگیرند. |
| **Flatten** | تبدیل Feature Map نهایی (خروجی لایه‌های کانولوشن) به یک **بردار یک‌بُعدی** برای آماده‌سازی جهت ورود به لایه‌های Dense. |
| **Dense Layers (لایه‌های Fully Connected)** | وظیفه **Classification (طبقه‌بندی)** نهایی را بر عهده دارند. این لایه‌ها بر اساس **ویژگی‌های استخراج‌شده** (نه پیکسل‌های خام) تصمیم‌گیری می‌کنند. |


---

### ۲. فلسفه معماری CNN (کاهش بعد مکانی، افزایش عمق)

معماری‌های موفق CNN (مانند AlexNet و YOLO) از یک الگوی تکراری پیروی می‌کنند: **کاهش ابعاد مکانی (Spatial Dimensions) در کنار افزایش عمق (Depth) شبکه.**

* **الگوی کاهشی:** ابعاد پیکسل‌های تصویر (مانند $N \times N$) به تدریج و به سرعت کاهش می‌یابد (مثلاً از $227 \times 227$ به $6 \times 6$).
* **افزایش عمق:** در مقابل، تعداد لایه‌ها یا کانال‌های Feature Map (عمق) افزایش می‌یابد (مثلاً از ۳ لایه ورودی به ۲۵۶ کانال خروجی).

#### دلیل این الگو:

1.  **تمرکز بر الگوهای محلی:** این رویکرد به شبکه اجازه می‌دهد تا از نگاه کردن به الگوهای کلی (Global Patterns) دست برداشته و بر **الگوهای محلی (Local Patterns)** و ویژگی‌های استخراج‌شده تمرکز کند.
2.  **استخراج ویژگی‌های متنوع:** هر یک از این کانال‌های عمقی جدید (Feature Mapها) می‌تواند برای تشخیص یک ویژگی خاص فعال شود؛ مثلاً یکی گوش تیز، دیگری دماغ دایره‌ای، یا چشم‌ها را استخراج کند.
3.  **بهره‌گیری از دانش موجود:** این الگو (کاهش $N \times N$ و افزایش عمق) یک **الگوی تکراری** در مقالات و معماری‌های موفق است و باید توسط طراحان شبکه مدنظر قرار گیرد.

---

### ۳. لایه‌های Pooling (کاهش ابعاد)

**Pooling (پولینگ)** یک لایه کمکی در CNN است که برای **کاهش ابعاد مکانی (Dimensionality Reduction)** Feature Mapها استفاده می‌شود.

#### الف) نحوه عملکرد Pooling:

1.  **هدف:** کاهش ابعاد Feature Map (مثلاً از $6 \times 6$ به $3 \times 3$) و کاهش بار محاسباتی در لایه‌های بعدی.
2.  **هایپرپارامترها:** Pooling نیز مانند کرنل دارای دو هایپرپارامتر است:
    * **Filter Size ($F$):** ابعاد پنجره‌ای که لایه به آن نگاه می‌کند (مثلاً $2 \times 2$ یا $3 \times 3$).
    * **Stride ($S$):** گام حرکت پنجره (مثلاً $S=2$).
3.  **مکانیزم:** لایه Pooling یک عملیات **غیرپارامتریک** است که بر روی پنجره $F \times F$ اعمال می‌شود.

#### ب) انواع Pooling:

1.  **Max Pooling (ماکس پولینگ):**
    * **کارکرد:** در هر پنجره، **بزرگترین (ماکسیموم) مقدار** را انتخاب و به عنوان نماینده آن ناحیه در خروجی قرار می‌دهد.
    * **کاربرد:** رایج‌ترین نوع در مقالات و معماری‌های معروف (مانند AlexNet).
2.  **Average Pooling (اوریج پولینگ):**
    * **کارکرد:** در هر پنجره، **میانگین (اوریج)** مقادیر را محاسبه کرده و به عنوان خروجی قرار می‌دهد.



#### ج) مزایا و معایب Pooling:

| عنوان | مزایا (Advantages) | معایب (Disadvantages) |
| :--- | :--- | :--- |
| **محاسبات** | **کاهش سریع ابعاد**، کاهش بار محاسباتی (FLOPS)، افزایش سرعت یادگیری. | –– |
| **آموزش** | کمک به **جلوگیری از بیش‌برازش (Overfitting)**، زیرا شبکه به جزئیات کمتر حساس می‌شود. | –– |
| **ویژگی‌ها** | افزایش قدرت شبکه در تشخیص **الگوهای مهم‌تر** (به ویژه Max Pooling) و کاهش حساسیت به **محل دقیق قرارگیری** یک جسم در تصویر. | می‌تواند ناخواسته باعث **از بین رفتن جزئیات** مهم و دقیق تصویر شود. |

#### د) نکته حیاتی (پارامترهای آموزشی):

* **تعداد پارامترهای آموزشی در لایه‌های Pooling برابر با صفر است.** (۰)
* Pooling لایه‌ای **غیرپارامتریک** است؛ یعنی هیچ وزن ($W$) یا بایاسی ($b$) برای یادگیری و آموزش ندارد و صرفاً یک عملیات ریاضی (ماکسیمم‌گیری یا میانگین‌گیری) انجام می‌دهد.

#### ه) مقادیر رایج هایپرپارامترها:

* رایج‌ترین ترکیب‌های مورد استفاده در مقالات:
    * **$F=2$ و $S=2$**
    * **$F=3$ و $S=2$** (مانند AlexNet)
* **قاعده کلی:** اگر نمی‌خواهیم جزئیات زیادی از بین برود، باید از پولینگ‌های **کوچک‌تر** استفاده کنیم.

---

### ۴. ملاحظات حرفه‌ای و عملی

1.  **الزام به مطالعه مقالات:** مسیر دیپ لرنینگ طولانی است و ما **نمی‌توانیم** تمام راه‌ها و آزمایش‌ها را خودمان تکرار کنیم. برای تسلط بر هایپرپارامترها، **حتماً** باید مقالات را مطالعه کرده و معماری‌های معروف (مانند AlexNet، VGG، YOLO) را بررسی کنیم تا الگوهای تکراری و موفق را شناسایی کنیم.
2.  **عدم وضوح کامل:** اگرچه لایه‌های کانولوشن برای استخراج ویژگی‌های قابل توجیه (مانند چشم و گوش) استفاده می‌شوند، اما در واقعیت، بسیاری از Feature Mapهای استخراج‌شده **معنی واضح و ملموسی** برای انسان ندارند و صرفاً "دارند کار می‌کنند" و در نهایت دقت بالایی ایجاد می‌کنند.
3.  **تفاوت با Stride در Conv:** هرچند استفاده از **Stride > 1 در لایه Conv** نیز کاهش بعد می‌دهد، اما Pooling یک روش **غیرپارامتریک** برای کاهش بعد است که بار محاسباتی را **به طور مستقیم** کاهش می‌دهد، در حالی که لایه Conv همچنان دارای پارامترهای قابل آموزش است.
