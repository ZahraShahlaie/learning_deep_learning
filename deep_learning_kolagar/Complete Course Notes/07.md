## 📚 مفاهیم اساسی آموزش شبکه عصبی: اِیپاک (Epoch) و بچ (Batch)

این جزوه بر اساس محتوای ارائه شده درباره مفاهیم **اِیپاک (Epoch)** و **بچ (Batch)** در فرآیند آموزش شبکه‌های عصبی (Neural Networks) تهیه شده است.

---

### ۱. مفهوم اِیپاک (Epoch)

**اِیپاک** به معنای **یک دور کامل** دیدن کل مجموعه داده آموزشی توسط شبکه عصبی است.

* **تعریف دقیق:** هر بار که شبکه عصبی **کل دیتاست** (Data Set) را از ابتدا تا انتها پردازش می‌کند، یک مقدار خطا (Error) محاسبه می‌شود، و در نهایت **یک به‌روزرسانی (Update)** کامل برای وزن‌ها و بایاس‌های شبکه انجام می‌پذیرد، یک ایپاک کامل شده است.
* **هدف:** هدف از تکرار چندین ایپاک، این است که مدل به تدریج خطا را کاهش داده و پارامترهای خود را برای بهبود عملکرد بهینه‌سازی کند.

---

### ۲. مفهوم بچ (Batch) و مینی-بچ (Mini-Batch)

**بچ (Batch)** در ابتدا به کل مجموعه داده اشاره دارد، اما در عمل و در شبکه‌های بزرگ، از مفهوم **مینی-بچ (Mini-Batch)** استفاده می‌شود.

#### الف. ضرورت استفاده از مینی-بچ

در حالت سنتی **گرادیان نزولی (Gradient Descent)**، شبکه باید **کل دیتاست** را ببیند تا میانگین خطاها (گرادیان) را محاسبه کرده و وزن‌ها را **یک بار** آپدیت کند. این رویکرد دو مشکل عمده دارد:

1.  **محدودیت حافظه (Memory Limitation):** در حجم داده‌های بسیار بزرگ (مانند یک ترابایت دیتا)، حافظه (به ویژه حافظه کارت گرافیک یا GPU) نمی‌تواند کل داده‌ها را همزمان در خود نگه دارد.
2.  **کندی سرعت (Slow Speed):** چون شبکه باید منتظر بماند تا کل داده‌ها پردازش شوند تا یک بار آپدیت انجام دهد، فرآیند آموزش بسیار زمان‌بر است.

#### ب. تعریف مینی-بچ و بچ سایز (Batch Size)

* **مینی-بچ:** بخشی کوچک و تقسیم‌شده از کل مجموعه داده است.
* **بچ سایز (Batch Size):** **تعداد نمونه‌هایی** است که در هر مرحله به شبکه داده می‌شود. این یک **هایپرپارامتر** قابل تنظیم است (مثلاً ۲۰، ۳۲، ۶۴ و...).
* **نحوه کار:** به جای دادن ۱ میلیون نمونه به طور همزمان، داده‌ها به دسته‌های کوچکتر (مثلاً ۵۰,۰۰۰ دسته ۲۰ تایی) تقسیم می‌شوند. شبکه هر بار یک مینی-بچ را پردازش می‌کند و پس از محاسبه خطای همان بچ، بلافاصله **وزن‌ها را آپدیت می‌کند**.

#### ج. رابطه ایپاک و مینی-بچ

* **یک ایپاک** زمانی کامل می‌شود که **تمام مینی-بچ‌ها** یک بار به شبکه داده شده باشند و کل دیتاست دیده شده باشد.
* **تعداد آپدیت در یک ایپاک:** اگر دیتاست شامل ۱۰۰ نمونه و بچ سایز ۲۰ باشد، ۵ مینی-بچ خواهیم داشت ($100 / 20 = 5$). بنابراین، در یک ایپاک، شبکه **۵ بار** وزن‌های خود را آپدیت می‌کند.

---

### ۳. حل مشکل سرعت و الگوریتم‌های بهینه‌سازی

اگرچه مینی-بچ‌ها منجر به آپدیت‌های بیشتری در یک ایپاک می‌شوند (مثلاً ۵۰ بار به جای ۱ بار)، اما در عمل سرعت آموزش را افزایش می‌دهند.

* **دلیل افزایش سرعت:** استفاده از مینی-بچ امکان بهره‌گیری حداکثری از **پردازش موازی (Parallel Processing)** را فراهم می‌کند. سخت‌افزارهایی مانند **GPU** دارای هزاران هسته هستند و می‌توانند نمونه‌های موجود در یک بچ را به صورت همزمان (نه متوالی) پردازش کنند. این مزیت سخت‌افزاری بر هزینه محاسباتی آپدیت‌های مکرر غلبه کرده و سرعت آموزش را بهبود می‌بخشد.

#### الگوریتم‌های مرتبط

* **گرادیان نزولی (Gradient Descent):** کل دیتاست را می‌بیند و یک بار آپدیت می‌کند. (عملاً در دیتاست‌های بزرگ منسوخ شده است).
* **گرادیان نزولی تصادفی (Stochastic Gradient Descent - SGD):** از مینی-بچ‌های تصادفی (به جای کل دیتاست) برای محاسبه گرادیان و به‌روزرسانی وزن‌ها استفاده می‌کند. این الگوریتم و سایر مشتقات آن (مانند Adam، RMSprop و...) توانسته‌اند مسئله کندی و محدودیت حافظه را حل کنند.

---

